# ===== AraLink Environment =====
PORT=3000

# Gemini API key — fallback translation engine (primary is free Google endpoint)
# Get one free at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# ===== مسار الفيديو: Gemini تجلب رابط يوتيوب بنفسها =====
# يحل حجب يوتيوب لعناوين مراكز البيانات: خوادم Google تجلب الفيديو لا خادمنا.
# هذا المسار هو الوحيد الذي يجعل روابط يوتيوب تعمل على Render، ويتطلب
# GEMINI_API_KEY أعلاه — بلا مفتاح تبقى الروابط معطّلة هناك.
# الحصة المجانية: 8 ساعات فيديو يوميًا. الفيديوهات العامة فقط.
# GEMINI_VIDEO=false                    # لتعطيل المسار صراحةً
# GEMINI_VIDEO_MODEL=gemini-2.5-flash
# MAX_VIDEO_MINUTES=20                  # ~300 رمز لكل ثانية فيديو — الحد يحمي حصتك

# نموذج Whisper للتفريغ الصوتي — الافتراضي whisper-small (244MB)؛ أدق بكثير على العربية/التركية.
# tiny (39MB) أسرع لكنه ضعيف على اللغات غير الإنجليزية؛ small هو الحد الأدنى المقبول.
# WHISPER_MODEL=Xenova/whisper-small

# حجم نموذج sherpa-onnx: tiny|base|small (الافتراضي tiny ~75MB int8).
# base (~150MB) وsmall (~244MB) أدق بكثير على العربية والتركية لكنهما أبطأ وأثقل.
# على أجهزة ذات ذاكرة محدودة، tiny أو base قد يكونان أفضل خيار.
# يُنزَّل النموذج تلقائيًا عند أول تشغيل ويُخزَّن في MODEL_DIR.
# SHERPA_WHISPER_VARIANT=tiny

# محرك التفريغ الصوتي: sherpa (أسرع بكثير — عبر sherpa-onnx، يُنزَّل النموذج تلقائيًا)
# أو transformers (الاحتياطي عبر Xenova whisper). إن لم يُثبَّت sherpa-onnx يُستخدم transformers تلقائيًا.
STT_ENGINE=sherpa

# مجلد النماذج (يُنزَّل فيه نموذج sherpa تلقائيًا عند أول تشغيل — لا يُدرج في git)
# MODEL_DIR=models

# مسار ملف كاش الترجمة (الافتراضي: cache/translation-cache.json)
# ⚠️ نظام ملفات Render المجاني مؤقت — الكاش يختفي عند كل إعادة نشر أو خمول.
# وجّهه إلى قرص دائم إن أردت بقاءه.
# CACHE_FILE=cache/translation-cache.json
# مدة صلاحية الكاش بالمللي ثانية (الافتراضي: 30 يومًا = 2592000000)
# اضبطها 0 لتعطيل الانتهاء (يبقى مقيدًا بالحد الأقصى للعدد فقط)
# CACHE_TTL_MS=2592000000

# ===== لوحة جودة الترجمة =====
# تقرير القياس الذي يكتبه `npm run bench:translate` وتقرأه لوحة التحكم
# (GET /api/stats/quality). الافتراضي: cache/quality-report.json
# CACHE على Render مؤقت — أعد تشغيل القياس بعد كل نشر أو وجّهه لقرص دائم.
# QUALITY_REPORT=cache/quality-report.json
# مجموعة الجُمل المرجعية (الافتراضي: samples/translation/refset.json)
# QUALITY_REFSET=samples/translation/refset.json

# ===== سجل التكاليف =====
# عداد تكاليف العمليات الثقيلة (مقاطع TTS، أحرف الترجمة، ثوانٍ ffmpeg)
# يُقرأ عبر GET /api/stats/cost في لوحة التحكم. الافتراضي: cache/cost.json
# COST_FILE=cache/cost.json

# مفتاح API اختياري للطلاب (إن ضُبط: الطلبات التي تحمله تحصل على حد أعلى ×3)
# يُرسَل في ترويسة `x-api-key` **فقط** — مسار الاستعلام ?api_key= مرفوض عمدًا
# لأنه يظهر في سجلات الخادم وسجل المتصفح وترويسة Referrer.
# ⚠️ مفتاح حصص فقط — لا يمنح أي صلاحية إدارية.
# ARALINK_API_KEY=


# ===== CORS =====
# قائمة بيضاء للأصول المسموحة عبر المتصفح (فاصلة لأكثر من أصل، أو '*' للكل).
# فارغة = نفس الأصل فقط (الافتراضي الآمن) — الواجهة تستخدم روابط نسبية بلا تأثير.
# CORS_ORIGIN=
# ===== رمز الإدارة — يفتح /api/settings (قراءة وكتابة .env وقواعد الاستخراج) =====
# إن لم يُضبط: مسار /api/settings معطّل بالكامل ويُرجع 503 (الافتراضي الآمن).
# اضبطه فقط إن كنت تحتاج نافذة الإعدادات، وأرسله في ترويسة x-admin-token.
# ⚠️ لا تضبطه أبدًا على نشر عام بلا سبب: من يملكه يستطيع تحويل مفاتيحك
# إلى خادم غريب عبر ZEN_BASE_URL.
# ولّده بأمر: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ADMIN_TOKEN=

# بريد MyMemory الاختياري — يرفع الحصة اليومية المجانية للترجمة
# MYMEMORY_EMAIL=you@example.com

# عنوان خادم LibreTranslate الاحتياطي (اختياري)
# LIBRE_URL=https://libretranslate.com

# ===== مزوّدات ترجمة اختيارية (مجانية) =====
# DeepL المجاني — مفتاح اختياري من deeple.com/pro-api (مجاني)
# DEEPL_API_KEY=
# opencode zen — بوابة متوافقة مع OpenAI (chat/completions بمصادقة Bearer).
# المفتاح من https://opencode.ai/zen — وفيها نماذج مجانية.
# ZEN_API_KEY=
# الافتراضي https://opencode.ai/zen/v1 — غيّره لأي خادم متوافق
# (Ollama: http://localhost:11434/v1 — LM Studio: http://localhost:1234/v1)
# ZEN_BASE_URL=https://opencode.ai/zen/v1
# ZEN_MODEL=deepseek-v4-flash-free
# ترتيب المزوّدين المفضّل (فاصلة) — تُتخطى المزوّدات غير المتوفرة تلقائيًا
# PROVIDER_ORDER=google,mymemory,libre,gemini

# حدود الطلبات (طلبات/دقيقة لكل IP)
RATE_LIMIT_MAX=20
RATE_LIMIT_MAX_HEAVY=10

# ===== تخزين عدّادات حد الطلبات (server/store.js) =====
# فارغ (الافتراضي) = عدّادات في ذاكرة كل عملية — مناسب لنسخة خادم واحدة.
# مع أكثر من نسخة (توسّع أفقي) اضبط REDIS_URL لمشاركة العدّادات، وإلا صار
# الحد الفعلي = RATE_LIMIT_MAX × عدد النسخ. الحزمة `redis` اختيارية: تُثبَّت
# مع `npm install`، وأي فشل اتصال يسقط تلقائيًا للذاكرة (تحذير في السجل).
# REDIS_URL=redis://localhost:6379

# سائق طابور الوظائف: 'memory' (افتراضي، نسخة واحدة بلا Redis)
# أو 'redis' مستقبلاً — يتطلب REDIS_URL وحزمة redis الاختيارية.
# QUEUE_DRIVER=memory

# ===== تخزين الكائنات (S3 / R2 / MinIO) — اختياري =====
# سائق التخزين الافتراضي: 'local' (ملفات على القرص).
# اضبطه على 's3' لتوجيه التخزين إلى S3-compatible bucket:
#   STORAGE_DRIVER=s3
#
# Cloudflare R2 (مجاني 10GB + لا رسوم نقل): أنشئ bucket من dashboard →
#   https://dash.cloudflare.com → R2 → Create bucket
#   ثم API Tokens → Create Token (S3 API permissions) → انسخ المفتاح والسر.
#   الإطار التلقائي: endpoint = https://<acct>.r2.cloudflarestorage.com, region = auto
#
# AWS S3: أنشئ bucket من console → S3 → Create bucket
#   ثم IAM → Users → Security credentials → Access keys
#
# MinIO (محلي): http://localhost:9000 — تتطلب S3_FORCE_PATH_STYLE=true
#
# ⚠️ بدون متغيرات كاملة: يسقط السائق تلقائيًا للمحلي مع تحذير في السجل.
# STORAGE_DRIVER=local
# S3_ENDPOINT=https://<acct>.r2.cloudflarestorage.com
# S3_REGION=auto
# S3_BUCKET=my-bucket
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_FORCE_PATH_STYLE=false
# S3_PREFIX=
# S3_PUBLIC_URL=

# أقصى مدة فيديو محلي بالدقائق (الافتراضي 5 — التفريغ الصوتي بطيء ~5.5x المدة)
# LOCAL_VIDEO_MAX_MIN=5


# ===== YouTube Data API v3 (المسار المتوافق) =====
# مفتاح من Google Cloud Console (مجاني: 10,000 وحدة/يوم، وطلب البيانات الوصفية
# يكلّف وحدة واحدة). يُفعّل GET /api/youtube/metadata.
#
# ⚠️ مهم: هذا المفتاح يعطي **البيانات الوصفية فقط**. نص الترجمات غير متاح رسميًا
# لفيديو لا تملكه: captions.download يتطلب OAuth بحساب مالك الفيديو ويعيد 403
# لأي طرف ثالث بالتصميم. لترجمة محتوى فيديو، ارفع الملف عبر /api/video-local.
YOUTUBE_API_KEY=
