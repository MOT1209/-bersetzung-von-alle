# Dependencies
node_modules/

# Environment
.env
.env.local

# Translation cache
cache/

# STT models (downloaded automatically — never commit)
models/

# Uploads (local storage)
public/uploads/

# OS / Editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Vendored starter-kit clones (their own git repos)
agentic-coding-starter-kit/
/skills/

# Local backup archives
*.zip

# OCR traineddata (تُحمَّل عبر npm run download:ocr — لا تُلتزم)
server/ocr/traineddata/

# عيّنات قياس التفريغ (صوت ومانيفست محلي)
samples/stt/*
!samples/stt/README.md

# Reports / benchmarks (local, not tracked)
turkish-ar.srt
تقرير-الفحص.html
تقرير-الفحص-الشامل.html
bun.lock

# Reports / benchmarks (wildcard - local generated reports)
reports/
reports/**
coverage/
