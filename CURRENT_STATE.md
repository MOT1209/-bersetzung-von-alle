# CURRENT_STATE.md — تقرير الحالة الراهنة (Phase 0: Repository Audit)

> **الغرض:** الفحص الكامل للمستودع قبل كتابة أي كود، تنفيذًا للقسم 2 والقسم 69 من
> Master Development Prompt. هذا التقرير هو **الأساس الواقعي** لأي قرار معماري لاحق.
>
> **التاريخ:** 2026-09-07 · **الالتزام المفحوص:** `8c3054a` · **الفرع:** `claude/work-review-aefh9k` (مطابق لـ `main`)
>
> **حالة التنفيذ:** أُنجزت **P0.5** و**P1** و**عميل YouTube** و**P2** و**P3** و**الاستوديو المرئي** و**إعداد النشر** (§11–§17). الأرقام أدناه محدَّثة لتعكسها.
>
> **منهجية الفحص:** قراءة الملفات فعليًا + تشغيل `npm run check` و`npm run lint` و`npm test` و`npm audit`.
> كل رقم أو ادّعاء هنا مُتحقَّق منه بتنفيذ فعلي، لا استنتاج. ما لم يُتحقَّق منه مُعلَّم صراحةً بـ **[افتراض]**.

---

## 1. ما هو موجود (Inventory)

### 1.1 الخادم — `server/` (36 وحدة، 6342 سطرًا)

| الوحدة | الأسطر | الدور |
|---|---:|---|
| `files.js` | 608 | استيراد/تصدير الملفات (11 استيراد → 8 تصدير) |
| `translate.js` | 551 | محرك الترجمة: 6 مزوّدين + كاش + تقسيم + cooldown |
| `routes-sse.js` | 409 | بثّ الترجمة عبر SSE |
| `routes-translate.js` | 380 | مسارات الترجمة (رابط/نص/ذكية/SRT) |
| `audio.js` | 354 | تفريغ صوتي (yt-dlp + ffmpeg + STT) |
| `fetchContent.js` | 304 | جلب المقالات/المواقع/PDF + حماية SSRF |
| `geminiVideo.js` | 301 | ترجمة فيديو يوتيوب عبر Gemini متعدد الوسائط |
| `cache.js` | 252 | كاش ترجمة على ملف JSON بكتابة ذرّية |
| `server.js` | 218 | تركيب Express: CORS، helmet، حد الطلبات، المسارات |
| `ssrf.js` | 211 | حماية SSRF + تثبيت الاتصال على العنوان المُتحقَّق |
| `routes-video.js` | 211 | بثّ الفيديو مع Range + عدّ مراجع |
| `store.js` | 182 | متجر KV قابل للتبديل (ذاكرة/Redis) لعدّادات الحد |
| `context-detect.js` | 178 | كشف نوع المحتوى (تقني/كود/طبي/قانوني…) |
| `languages.js` | 176 | ~130 لغة بأسماء عربية |
| `quality.js` / `wer.js` | 159 / 97 | قياس جودة الترجمة بـ WER مع تطبيع عربي/تركي |
| `envSettings.js` | 159 | قراءة/كتابة `.env` بأمان (allowlist + منع حقن الأسطر) |
| `pdf.js` | 149 | استخراج نص PDF بلا مكتبات (zlib فقط) |
| `stats.js` / `usage.js` / `logger.js` | 139 / 55 / 45 | إحصاءات + عدّاد استخدام + سجل أخطاء بتدوير |
| `tashkeel.js` | 130 | تشكيل عربي (Gemini + احتياطي قواعدي) |
| `routes-local-video.js` | 129 | فيديو محلي base64 → تفريغ → ترجمة |
| `tts.js` | 126 | نطق عبر gTTS + دمج ffmpeg |
| `extractionRules.js` | 98 | قواعد استخراج CSS مخصصة للمواقع الصعبة |
| `routes-*.js` (بقية) | ~430 | file / dub / tts / ocr / tashkeel / stats / settings |
| `downloader.js` | 79 | غلاف yt-dlp عبر execFile |
| `ocr.js` | 76 | OCR عبر tesseract.js (ara+eng) بمهلة 60 ثانية |
| `youtube.js` | 67 | استخراج معرف الفيديو + جلب الترجمات النصية |

### 1.2 الواجهة — `public/` (2651 سطرًا)
- `index.html` (312) + `admin.html` (83) + `style.css`
- وحدات ES: `app.js` (نقطة الدخول), `translate.js`, `result.js`, `media.js`, `dub.js`, `features.js`, `ui.js`, `stream.js`, `dashboard.js`, `utils.js`, `constants.js`
- PWA: `sw.js` + `manifest.webmanifest` + `theme-init.js`

### 1.3 الإضافة — `extension/` (Chrome MV3)
- صلاحية `activeTab` فقط (لا `<all_urls>`) — منضبطة أمنيًا.
- `background.js`: قائمة سياقية تفتح AraLink بـ `?url=`. `popup.js`: زر «ترجم الصفحة الحالية».
- **لا تنفّذ أي معالجة** — تحوّل فقط إلى الويب. (مطابق لمتطلب القسم 35 مسبقًا.)

### 1.4 البنية التحتية والتوثيق
- `Dockerfile` (node:22-slim + ffmpeg + yt-dlp عبر pip3، فحص وقت البناء، healthcheck)
- `docker-compose.yml` (منفذ 3999، volume للكاش) · `render.yaml` (خطة free، healthCheckPath)
- `.github/workflows/ci.yml`: check → lint → test → docker build
- `AGENTS.md`, `CLAUDE.md`, `DESIGN.md`, `README.md`, `CHANGELOG.md`, `plan.md`
- `specs/`: 7 مجلدات موجات (wave0…wave3، audio-pipeline، speed-video-langs، translation-tool)
- `.agents/skills/` + `.claude/skills/`: 27 مهارة

---

## 2. ما يعمل (مُتحقَّق منه بالتشغيل)

| الفحص | النتيجة |
|---|---|
| `npm run check` (فحص صياغة) | **53/53 ملفًا سليمًا** |
| `npm run lint` (eslint `--max-warnings=0`) | **PASS — صفر تحذير** |
| `npm test` | **359/359 اختبارًا ناجحًا، صفر فشل** (39 ملف اختبار) |

**وظائف تعمل فعليًا:**
- **الترجمة:** 6 مزوّدين مسجَّلين (`google`, `mymemory`, `libre`, `gemini`, `deepl`, `zen`) بسلسلة احتياطية + تجميد 60 ثانية بعد 3 إخفاقات + كاش على القرص.
- **البثّ SSE:** `/api/translate-stream` مركَّب **قبل** `compression()` (اختبار يحرس ذلك)، ومحاذاة 1:1 صارمة في كل المسارات.
- **يوتيوب:** ثلاثة مسارات متتالية — Gemini video → ترجمات يوتيوب النصية → تفريغ صوتي (Whisper/sherpa).
- **الملفات:** استيراد 11 صيغة (txt, md, docx, xlsx, csv, srt, vtt, json, xml, epub, pptx) وتصدير 8.
- **الدبلجة:** `/api/dub` بدفعات ≤40 مقطعًا، توازٍ 4، بوابة لغة TTS.
- **OCR:** tesseract.js (ara+eng) بمهلة 60 ثانية عبر `Promise.race` (`ocr.js:35-46`).
- **الأمن:** helmet + CSP، CORS allowlist، حد طلبات على `/api` كامل، SSRF مع تثبيت الاتصال، مقارنة توكن الأدمن ثابتة الزمن، حجب المفاتيح في رسائل الخطأ.
- **خريطة المسارات:** كل نقطة تستدعيها الواجهة لها مسار مسجَّل (`tests/smoke.test.js` يفشل إن انكسر ذلك).

---

## 3. ما لا يعمل / معطَّل

| البند | الحالة | الدليل |
|---|---|---|
| `msedge-tts` | ~~تبعية ميتة~~ — **أُزيلت في P0.5** | كانت مُعلنة وغير مستوردة؛ `tts.js` يستخدم gTTS |
| تنزيل يوتيوب المباشر على السحابة | **محجوب فعليًا** على Render | `downloader.js:57` يكتشف الحجب ويرفع `youtube-blocked` |
| تصدير PPTX / EPUB | **غير مدعوم عمدًا** (يُخرَجان txt/md/docx) | `files.js:15-16` |
| `bench:translate` | لا يعمل في CI عمدًا (يستهلك حصص مجانية) | `AGENTS.md` |
| توثيق TTS | ~~انحراف توثيقي~~ — **صُحِّح في P0.5** | `AGENTS.md:43` صار يذكر gTTS |

---

## 4. ما يحتاج refactor

1. **`server.js` صار موزّع المسؤوليات** (218 سطرًا): تركيب المسارات + حد الطلبات + المصادقة + CORS + معالجة الأخطاء في ملف واحد. ترتيب الوسائط حسّاس جدًا (SSE قبل الضغط، حدود الأجسام قبل `express.json` العام) وموثَّق بتعليقات طويلة — هشّ أمام أي إضافة.
2. **`files.js` (608 سطرًا)** يجمع 11 مستوردًا + 8 مُصدِّرًا في وحدة واحدة بـ `switch` كبير. يجب فصله إلى `importers/` و`exporters/` بواجهة موحّدة.
3. ~~**`translate.js` (551 سطرًا)** يخلط سجل المزوّدين ومنطق كل مزوّد~~ — **عولج في P1**: صار 302 سطرًا، والمزوّدون في `providers/translation/*.js`.
4. **ازدواج منطق البثّ**: `routes-translate.js` و`routes-sse.js` ينفّذان الأنبوب نفسه مرتين (يوتيوب/مقال/نص). أُصلحت المحاذاة بإعادة استخدام `translateBatch`، لكن بقية التدفق ما زال مكرَّرًا.
5. **الوصول وقت التنفيذ كنمط اختبار** (`const f = (...a) => mod.f(...a)`) منتشر لأن التفكيك يجمّد المراجع — حلٌّ صحيح لكنه عرض لغياب حقن التبعيات (DI).

---

## 5. ما ينقص (الفجوة بين الحالي والهدف)

مقارنة بالمنصة المطلوبة في الـMaster Prompt:

| المطلوب | الحالة الآن |
|---|---|
| Auth / حسابات مستخدمين | **غير موجود** — لا مستخدمين، لا جلسات، لا صلاحيات |
| قاعدة بيانات + Models | ~~غير موجودة~~ — **أُضيفت في P3**: SQLite عبر `node:sqlite` بترحيلات (المشاريع والأصول) |
| Job Queue + Workers | ~~غير موجود~~ — **أُضيف في P2** (الاستئناف بعد إعادة التشغيل ما زال مفتوحًا) |
| نظام Projects | ~~غير موجود~~ — **أُضيف في P3**: `/api/projects` كامل مع أصول |
| Speaker Diarization | **غير موجود** |
| WhisperX / word-level timestamps | **غير موجود** (الحالي: sherpa-onnx / Xenova whisper-tiny) |
| Translation Memory | **جزئي** — الكاش يخزّن قطعًا مترجمة لكن بلا لغة/مشروع/ثقة |
| Glossary | **موجود جزئيًا** — `applyGlossary` بعد الترجمة فقط، لا يُحترم أثناءها |
| Subtitle: ASS، تصحيح توقيت، سرعة قراءة | **غير موجود** (SRT/VTT فقط) |
| Subtitle Editor (تحرير يدوي) | **غير موجود** |
| Voice Assignment / Cloning | **غير موجود** — صوت واحد لكل لغة |
| Audio Mixing (Replace/Mix/Voice-over) | **غير موجود** — الواجهة تشغّل mp3 بالتتابع |
| مطّ الصوت (`atempo`) لمزامنة التوقيت | **غير موجود** — تحقّقت: لا ذكر لـ`atempo` في المستودع |
| Video rendering / حرق الترجمات | **غير موجود** — يوجد بثّ فقط |
| Storage abstraction (S3/R2) | ~~غير موجود~~ — **أُضيف في P3**: `StorageProvider` بسائق محلي؛ S3/R2 ملف واحد لاحقًا |
| Batch processing | **غير موجود** |
| AI Quality Review (بعد الترجمة) | **غير موجود** — `quality.js` قياس offline لا مراجعة لكل ترجمة |
| Observability / structured logging | **بدائي** — `console.error` + ملف أخطاء نصي |
| Lip Sync | **غير موجود** (وهو Advanced حسب القسم 24) |

---

## 6. Dependencies الحالية

**إنتاج (18):** `@xenova/transformers`, `cheerio`, `compression`, `cors`, `docx`, `dotenv`, `epub2`, `exceljs`, `express`, `fast-xml-parser`, `helmet`, `jszip`, `mammoth`, `msedge-tts`(ميتة), `sherpa-onnx`, `tesseract.js`, `undici`, `youtube-transcript`
**اختيارية (2):** `redis`, `youtube-dl-exec` · **تطوير (1):** `eslint` · **المُثبَّت فعليًا:** 346 حزمة

### ⚠️ التدقيق الأمني — `npm audit --omit=dev`

**قبل P0.5: 13 ثغرة (1 حرجة + 6 عالية + 6 متوسطة) · بعد P0.5: 11 ثغرة (1 حرجة + 4 عالية + 6 متوسطة)**

| الخطورة | الحزمة | المشكلة |
|---|---|---|
| **CRITICAL** | `protobufjs` | تنفيذ كود عشوائي (عبر `@xenova/transformers` → `onnxruntime-web` → `onnx-proto`) |
| ~~HIGH~~ | ~~`adm-zip` (عبر `epub2`)~~ | **أُزيلت في P0.5** — حُذفت `epub2` وحلّ محلها JSZip بميزانية فكّ ضغط |
| HIGH | `sharp` | CVE-2026-33327/33328/35590/35591 في libvips |
| HIGH | `@xenova/transformers`, `onnxruntime-web`, `onnx-proto` | موروثة |

> **الأهم عمليًا (عولج):** كانت ثغرة `adm-zip` **قابلة للوصول مباشرةً** — `/api/translate-file` يقبل EPUB من المستخدم و`epub2` يفكّه عبر `adm-zip`. أُغلقت في P0.5.
>
> **ما يتبقّى:** كل الثغرات المتبقية العالية والحرجة تأتي من سلسلة واحدة: `@xenova/transformers` → `onnxruntime-web` → `onnx-proto` → `protobufjs`. **قابلية الوصول منخفضة** (تتطلب نموذج ONNX خبيثًا، والنماذج تُجلب من Hugging Face بطلب التطبيق نفسه لا من المستخدم)، والحزمة **محرّك STT احتياطي** فقط (`sherpa-onnx` هو الافتراضي). لا إصلاح أعلى المجرى: `2.17.2` هو الأحدث. الخليفة `@huggingface/transformers@4.x` يُسقط `onnx-proto`/`protobufjs` كليًا — انتقال يستحق التنفيذ لكنه يحتاج تحقّقًا فعليًا من التفريغ الصوتي، فلا يصح ضمّه إلى P0.5 بلا اختبار.

---

## 7. المشاكل المعمارية

1. ~~**العمليات الثقيلة داخل طلب HTTP.**~~ — **عولج في P2 (§14)**؛ النص أدناه يبقى لتوثيق التشخيص الأصلي.
    التفريغ الصوتي والدبلجة وOCR وتنزيل الفيديو كلها تعمل داخل دورة الطلب. **تصحيح فني مهم:** هي **لا تحجب** حلقة أحداث Node (تستخدم `execFile` = عملية نظام منفصلة، وtesseract.js يعمل في worker خاص). المشكلة الحقيقية ليست حجب الحلقة بل: **لا سقف للتزامن، لا استئناف، لا إلغاء، ومهلة الطلب تقتل العمل الجاري**. فيديو 8 دقائق عبر Gemini يعني طلب HTTP مفتوحًا 8 دقائق.
2. **الحالة على القرص لا في مخزن مشترك.** الكاش والإحصاءات والاستخدام ما زالت ملفات JSON محلية ⇒ **لا يمكن التوسّع أفقيًا**. `store.js` حلّها لعدّادات الحد، و**P3 حلّها للمشاريع والأصول** (SQLite + طبقة تخزين)؛ بقيت الكاش والإحصاءات.
3. ~~**لا طبقة providers حقيقية إلا للترجمة.**~~ — **عولج في P1**: الترجمة وSTT وTTS كلها خلف واجهات في `server/providers/`.
4. **الاعتماد على نقاط نهاية غير رسمية كمسار أساسي.** `translate.googleapis.com` (ترجمة+كشف لغة) و`translate.google.com/translate_tts` (نطق) بلا اتفاقية استخدام.
5. **ازدواج الأنبوب** بين المسار المتدفّق وغير المتدفّق (انظر §4.4).
6. **لا حدود موارد إجمالية.** حد الطلبات لكل IP فقط؛ لا سقف لعدد عمليات ffmpeg/yt-dlp المتزامنة ⇒ عشرة طلبات فيديو متزامنة تستهلك المعالج بالكامل.

---

## 8. Technical Debt

1. ~~`msedge-tts` تبعية ميتة~~ — **أُزيلت (P0.5)**.
2. 11 ثغرة في تبعيات الإنتاج (§6) — **القابلة للوصول أُغلقت (P0.5)**؛ الباقي من سلسلة `@xenova/transformers`.
3. ~~انحراف توثيقي في `AGENTS.md` حول TTS~~ — **صُحِّح (P0.5)**.
4. ~~تعليق قديم في `routes-settings.js:2`~~ — **صُحِّح (P0.5)**.
5. ~~ترتيب الوسائط في `server.js` هشّ ومحروس بالتعليقات لا بالبنية~~ — **حُرِس باختبار
   `tests/middlewareOrder.test.js` (§19)**: أي إعادة ترتيب تكسر SSE أو حدود الأجسام تفشل الآن.
6. ~~`public/js/dashboard.js` يحفظ توكن الأدمن في `localStorage`~~ — **صار كوكي `httpOnly`
   عبر `/api/admin/login` (§19)**.
7. ~~`specs/` بلا فهرس~~ — **أُضيف `specs/README.md` (§19)** يوسم كل موجة: منفَّذة/جزئية/مهجورة.
8. ~~لا `THIRD_PARTY.md`~~ — **أُضيف (§19)**، مولَّد من رخص `node_modules` الفعلية.
9. لا مصادقة مستخدمين حقيقية: ملكية المشروع توكن قدرة في المتصفح (§19). فقدان
   التخزين المحلي = فقدان الوصول إلى المشروع، ولا استرجاع. مقبول للـMVP، ويُستبدل
   بحسابات حقيقية عند الحاجة.

---

## 9. خطة الانتقال إلى المعمارية الجديدة

### 9.1 قرار محجوب يسبق كل شيء (Blocking Decision)

قبل أي عمل معماري يجب حسم تعارض صريح داخل المتطلبات نفسها:

- **القسم 63** يقول: «لا تتجاوز access controls… ولا تجعل النظام مصممًا لتجاوز حماية المحتوى».
- **الكود الحالي** (`geminiVideo.js:3-6`) يقول صراحةً إن غرضه **تجاوز حجب يوتيوب لعناوين مراكز البيانات**، و`downloader.js` يرصد الحجب ويسمّيه.

هذان لا يجتمعان. الخيارات:
- **(أ) مسار متوافق:** YouTube Data API الرسمي + محتوى يرفعه المستخدم بنفسه. أبطأ وأضيق وأغلى، لكنه قابل للتشغيل تجاريًا.
- **(ب) أداة محلية/شخصية:** إبقاء المسار الحالي وعدم تقديمها كخدمة عامة.

**لا توجد توصية تقنية صحيحة قبل هذا القرار** — لأنه يحدّد إن كان «YouTube Engine» في القسم 6 يُبنى أصلًا وبأي شكل.

> **القرار المتَّخذ (2026-09-07): المسار (أ) — متوافق.** YouTube Data API الرسمي للبيانات الوصفية + محتوى يرفعه المستخدم بنفسه.
> **الأثر المباشر:** `geminiVideo.js` (تمرير رابط يوتيوب إلى Gemini) و`downloader.js` (yt-dlp على روابط يوتيوب) **لا يصلحان** كأساس للمنتج العام ويجب عزلهما خلف علم صريح أو إزالتهما عند بناء المحرك الجديد. الأولوية تنتقل إلى مسار «ارفع ملفك» الذي يعمل أصلًا (`/api/video-local`).

### 9.2 التسلسل المقترح (بعد حسم 9.1)

| المرحلة | العمل | لماذا الآن |
|---|---|---|
| ~~**P0.5 — عاجل**~~ ✅ | إصلاح الثغرة القابلة للوصول، وحذف التبعيات الميتة، وتصحيح التوثيق | **مُنجَز** — انظر §11 |
| **P1 — Provider Layer** | استخراج `providers/{translation,stt,tts}/` بواجهات موحّدة (القسم 43) | يفتح كل ما بعده؛ لا يكسر أي API قائمة |
| **P2 — Job Queue** | Redis + BullMQ، تحويل المسارات الثقيلة إلى وظائف، SSE للتقدّم (الأقسام 5/26/27) | يحلّ §7.1 و§7.6 معًا، وشرط لأي عمل ثقيل حقيقي |
| **P3 — Storage + DB** | `StorageProvider` + قاعدة بيانات للمشاريع/المستخدمين (الأقسام 29/30/40) | شرط Projects وBatch وAuth |
| **P4 — Transcription** | واجهة STT + WhisperX/diarization خلفها (الأقسام 8/9) | يعتمد على P1 وP2 |
| **P5 — Subtitle + Dubbing** | ASS، تصحيح التوقيت، `atempo`، إسناد الأصوات، المزج (الأقسام 14-22) | يعتمد على P4 (المتحدثون) وP2 (ffmpeg الثقيل) |
| **P6 — Studio UI** | واجهات Projects/Editor (الأقسام 15/46-49) | بعد استقرار البيانات والوظائف |

### 9.3 قواعد ملزمة أثناء الانتقال
- **لا إعادة بناء الموجود** (القسم 3): الترجمة والكاش وSSRF والملفات وOCR تُغلَّف وتُطوَّر لا تُستبدل.
- **لا كسر APIs الحالية** بلا migration — 359 اختبارًا تحرسها، وأي انخفاض في العدد يعني انحدارًا.
- **البوابة بين المراحل:** `npm run check && npm run lint && npm test` خضراء + الميزة تعمل يدويًا قبل الانتقال (القسم 72).

---

## 10. الخلاصة بسطر واحد

المشروع **أنضج بكثير مما يبدو من حجمه**: 359 اختبارًا خضراء، أمان مدروس (SSRF، helmet، توكن ثابت الزمن، كتابة `.env` محصّنة)، وستة مزوّدي ترجمة باحتياطي حقيقي. الفجوة إلى «المنصة» ليست في جودة الكود القائم بل في **غياب أربع طبقات**: الوظائف (Queue)، والتخزين (DB/Storage)، والهوية (Auth)، وطبقة المزوّدين لغير الترجمة — إضافةً إلى **قرار قانوني** يسبقها كلها.


---

## 11. ما نُفِّذ بعد هذا الفحص — P0.5 (2026-09-07)

**الهدف:** إغلاق الضرر القائم فقط، بلا لمس المعمارية وبلا انتظار قرارات لاحقة.

1. **إغلاق ثغرة قابلة للاستغلال عبر رفع ملف.** حُذفت `epub2` (ومعها `adm-zip`) وأُعيدت
   قراءة EPUB فوق `JSZip` الموجودة أصلًا: `META-INF/container.xml` → ملف OPF →
   ترتيب `<spine>` → فصول XHTML. أُضيفت **ميزانية فكّ ضغط صريحة** (64MB لكل ملف)
   ترفض بناءً على الحجم المُعلَن في فهرس ZIP قبل تخصيص الذاكرة ثم تحاسب الإجمالي
   فعليًا — وطُبِّقت على قارئ PPTX أيضًا لأنه يتعرّض للفئة نفسها.
2. **تغطية اختبارية من الصفر لمسار EPUB.** لم يكن له اختبار واحد رغم أنه يستقبل
   ملفات المستخدم. أُضيف `tests/epub.test.js` (14 اختبارًا): ترتيب `spine`، وOPF في
   الجذر، وفكّ الكيانات، وإسقاط `script`/`style`، والفشل الصريح على الأرشيف التالف
   وعلى غياب `container.xml` وعلى `spine` الفارغ، وميزانية فكّ الضغط، وتطبيع المسارات.
3. **إصلاح تسريب اكتُشف أثناء الاختبار:** نص `<script>`/`<style>` كان يتسرّب إلى
   النص المُرسَل للترجمة، ومسافة دخيلة كانت تتصدّر كل فقرة.
4. **حذف `msedge-tts`** — تبعية مُعلنة وغير مستوردة إطلاقًا.
5. **تصحيح انحرافين توثيقيين:** `AGENTS.md` (TTS هو gTTS لا MS Edge TTS)،
   وتعليق `routes-settings.js` (المسار محمي بـ `requireAdmin` + `heavyLimiter`).

**التحقق:** `check` 53/53 · `lint` نظيف بصفر تحذير · **373/373 اختبارًا ناجحًا**
(كانت 359) · إقلاع الخادم و`/api/health` و`/api/languages` (132 لغة) مُتحقَّق منها فعليًا.

**النتيجة الأمنية:** 13 ثغرة → **11**، والأهم أن **الوحيدة القابلة للوصول من مدخلات
المستخدم أُغلقت**. المتبقّي محصور في سلسلة `@xenova/transformers` (انظر §6).


---

## 12. ما نُفِّذ — P1: طبقة المزوّدين (2026-09-07)

**الهدف (القسم 43):** كل مزوّد خارجي يُنفِّذ واجهة موحّدة، فإضافة مزوّد جديد =
ملف واحد + سطر في `index.js` — **بلا تعديل في النواة**. بلا كسر أي API قائمة.

### البنية الناتجة

```text
server/providers/
├── README.md              ← عقد الواجهات الثلاث (TranslationProvider/TTSProvider/TranscriptionProvider)
├── translation/           ← google · mymemory · libre · gemini · deepl · zen  (+ endpoints.js مشترك)
├── tts/                   ← gtts
└── stt/                   ← sherpa · transformers  (+ lang.js مشترك)
```

### الأثر على النواة

| الملف | قبل | بعد | ما بقي فيه |
|---|---:|---:|---|
| `translate.js` | 551 | **302** | الكاش، التقسيم، سلسلة الاحتياطي، التجميد، المسرد، كشف اللغة |
| `audio.js` | 354 | **183** | الملفات المؤقتة، ffmpeg→PCM، اختيار السلسلة، توحيد المقاطع |
| `tts.js` | 126 | **154** | حدود المدخلات، التقسيم على حدود الجمل، دمج ffmpeg |

`tts.js` كبر قليلًا لأنه اكتسب سجلّ مزوّدين حقيقيًا (كان مزوّدًا واحدًا مثبّتًا بالكود).

### قرارات تصميم

- **`isAvailable()` يُقرأ وقت الاستدعاء لا وقت الاستيراد.** قراءتها عند الاستيراد
  تُجمّد القيمة، فيصبح حفظ مفتاح جديد من لوحة الإعدادات بلا أثر حتى إعادة التشغيل
  (`envSettings.saveSettings` يعدّل `config` مباشرةً). موثَّق كقاعدة ملزمة في README.
- **الترتيب في `index.js` هو الأولوية الافتراضية** — لا ترتيب مبعثر في النواة.
- **سلسلة STT معمّمة:** المفضّل في `STT_ENGINE` أولًا ثم الباقي احتياطيًا، والمحرك
  غير المثبَّت يسقط تلقائيًا. الخطأ المُصنَّف (`audio-empty`) يتوقف فورًا، وغير
  المُصنَّف (نموذج/تحميل) ينتقل للمحرك التالي — تعميم للسلوك السابق لا تغيير له.
- **كل مزوّد يملك توحيد مخرجاته:** `toSegments` انتقلت إلى كل محرك STT لأن شكل
  المخرَج خاصية المزوّد لا النواة.

### تغطية اختبارية جديدة على منطق لم يكن مُختبَرًا إطلاقًا

محرّكا التفريغ يحتاجان نماذج وحزمًا أصلية فلا يمكن تشغيلهما في الاختبارات، لكن
**منطق الاختيار والتوحيد نقيّ وقابل للاختبار** — وأُضيف له `tests/sttProviders.test.js`
(16 اختبارًا): السجل، وتحقيق الواجهة، وسقوط المحرك غير المتاح من السلسلة، وحدود
المدة (2–10 ثوانٍ)، ودمج المقاطع الصامتة، وتوحيد مخرجات كل محرك، وتطبيع اللغات.

**التحقق:** `check` 67/67 · `lint` نظيف · **389/389 اختبارًا** (كانت 373) ·
الخادم يقلع و`GET /api/providers` يعيد المزوّدات الستة بنفس الشكل السابق.

### ما لم يتغيّر عمدًا

`LLMProvider` و`StorageProvider` (القسم 43) مؤجَّلان إلى P3 لأنهما بلا مستهلك اليوم:
لا تخزين كائنات ولا طبقة LLM مستقلة عن الترجمة. بناؤهما الآن تجريد بلا حاجة.


---

## 13. YouTube Data API الرسمي — بداية المسار المتوافق (2026-09-07)

تنفيذ لقرار §9.1 (المسار أ). البنود 1-3 من القسم 6 في خطة المنصة: التحقق من
الرابط، وجلب البيانات الوصفية (العنوان، الوصف، القناة، المدة، الصورة المصغّرة).

### حقيقة تحدّد المعمارية (مُتحقَّق منها من التوثيق لا من الذاكرة)

| القدرة | الحالة رسميًا |
|---|---|
| `videos.list` (بيانات وصفية) | ✅ **مفتاح API وحده يكفي** للفيديوهات العامة · **وحدة واحدة** من حصة 10,000/يوم |
| `captions.download` (نص الترجمات) | ❌ **يتطلب OAuth بحساب مالك الفيديو**. أي طرف ثالث يحصل على **403 بالتصميم** |

**لا التفاف على هذا:** لا نطاق OAuth أوسع ولا حساب خدمة يتجاوز فحص الملكية. لذلك
**لا توجد ولن تُضاف** دالة لتنزيل الترجمات في `youtubeApi.js`.

### الأثر العملي على المنتج

المسار المتوافق ينقسم صراحةً:
- **البيانات الوصفية** ← الواجهة الرسمية (`youtubeApi.js`).
- **المحتوى** ← ملف يرفعه المستخدم (`/api/video-local` — يعمل أصلًا)، لأنه يملك
  حق معالجة ما يرفعه.

الاستجابة تحمل هذا العقد صراحةً: `captionsAvailableToUs: false` و
`contentSource: 'upload-required'`. حقل `hasCaptions` يخبر أن للفيديو ترجمات
(من `contentDetails.caption`، بلا تكلفة إضافية) — لكنه **لا يعني** أن نصّها متاح
لنا، وهناك اختبار يحرس ألّا يدّعي المسار غير ذلك.

### ما أُضيف

- `server/youtubeApi.js` — العميل: تطبيع المعرّف، تحويل مدة ISO 8601 إلى ثوانٍ،
  اختيار أفضل صورة مصغّرة، وتمييز الأخطاء (`youtube-quota` عند 403 مقابل
  `youtube-api-failed` العام — علاجهما مختلف تمامًا).
- `server/routes-youtube.js` + `GET /api/youtube/metadata` تحت `heavyLimiter`
  (طلب رخيص لكنه يستهلك حصة خارجية).
- `YOUTUBE_API_KEY` في `config.js` و`.env.example` وقائمة مفاتيح لوحة الإعدادات،
  و`YOUTUBE_API_BASE` قابل للتوجيه كي تعمل الاختبارات بلا شبكة.
- `tests/youtubeApi.test.js` (13 اختبارًا) بخادم stub محلي.

**الافتراضي الآمن:** بلا مفتاح يعيد المسار `503 youtube-api-disabled` — الميزة
معطّلة لا مكسورة.

**التحقق:** `check` 69/69 · `lint` نظيف · **402/402 اختبارًا** (كانت 389) ·
مُتحقَّق حيًّا: 503 بلا مفتاح، و400 بلا معامل `url`.


---

## 14. ما نُفِّذ — P2: محرك الوظائف (2026-09-07)

### لماذا ليس BullMQ — قرار مُعلَن

الخطة تقول «Redis + BullMQ». لكن **BullMQ يفرض Redis ولا يملك وضع ذاكرة**، بينما:
- قاعدة المشروع في `AGENTS.md`: Redis **اختياري** ولا يجوز أن يكون شرطًا،
- والنشر الفعلي على Render المجاني نسخة واحدة **بلا Redis**.

فرض Redis كان سيكسر النشر القائم لحلّ مشكلة لم تظهر بعد (التوسّع الأفقي). لذا:
**محرك بواجهة سائق** — سائق داخل العملية الآن، وسائق BullMQ يُركَّب لاحقًا عند
الحاجة الفعلية **دون تغيير الواجهة ولا المسارات**. نفس فلسفة `store.js` بالضبط.

### ما حلّه فعلًا

التشخيص في §7.1 كان أن العمل الثقيل **لا يحجب** حلقة أحداث Node (execFile عملية
منفصلة)، والمشاكل الحقيقية أربع — وهذه حالتها الآن:

| المشكلة | الحل |
|---|---|
| لا سقف للتزامن | `JOB_CONCURRENCY` (افتراضي 2) يحكم **كل** تنفيذ، حتى المسار المتزامن |
| لا إلغاء | `DELETE /api/jobs/:id` — فوري ومؤكَّد قبل التشغيل، وتعاوني عبر `signal` أثناءه |
| لا متابعة | `GET /api/jobs/:id` وبثّ `/api/jobs/:id/stream` بمراحل ونسب |
| مهلة الطلب تقتل العمل | `{ async: true }` يعيد 202 ومعرّفًا، فيتحرّر العمل من عمر الطلب |
| (جديد) نموّ ذاكرة غير محدود | `JOB_MAX_QUEUED` يرفض بـ`queue-full`، و`JOB_TTL_MS` ينظّف المنتهية |

### التوافق الخلفي

`POST /api/video-local` **لم يتغيّر عقده**: نفس الجسم ونفس رموز الأخطاء (مع
`maxMinutes` محفوظًا). الفارق أن تنفيذه صار يمرّ بالطابور، فيستفيد المسار القديم
من سقف التزامن بلا أي تعديل من العميل. الاختبارات الستّة القائمة تمرّ **بلا تغيير
حرف واحد** — وهو الدليل العملي على أن العقد لم يُكسر (البند 70).

### درسان طُبِّقا من تاريخ المشروع نفسه

1. **`/api/jobs/:id/stream` مُركَّب قبل `compression()`** — الضاغط يخزّن
   `text/event-stream` فيصل البثّ دفعة واحدة في النهاية (يعمل الطلب ويضيع البثّ
   صامتًا). هناك اختبار يفشل إن ظهر `Content-Encoding` على البثّ.
2. **انقطاع العميل يُكتشف على `res` لا `req`** — منذ Node 16 يُطلق الطلب `close`
   فور قراءة جسمه.

### خلل حقيقي أمسكه الاختبار أثناء البناء

`enqueue` كان يشغّل الوظيفة **متزامنًا**، فيترتّب عليه أمران: يعيد وظيفة «تعمل»
لا «منتظرة**، والأخطر أن من يشترك بالتحديثات مباشرةً بعد `enqueue` (وهو بالضبط ما
يفعله بثّ SSE) كان **يفوته كل حدث** يقع قبل اشتراكه — ومعالج سريع ينتهي قبل أن
يبدأ الاستماع أصلًا. عولج بتأجيل التشغيل إلى `queueMicrotask`.

### الملفات

- `server/jobs/queue.js` (259 سطرًا) — المحرك: دورة الحياة، سقف التزامن، FIFO،
  التقدّم، الإلغاء، سقف الطابور، تنظيف TTL، أحداث للاشتراك.
- `server/jobs/index.js` — النسخة المشتركة؛ المعالجات تُسجَّل من الوحدة التي
  تملك منطقها (لا استيراد دائري).
- `server/routes-jobs.js` — المتابعة والبثّ والإلغاء والإحصاءات.
- `tests/jobsQueue.test.js` (18) + `tests/jobsApi.test.js` (9).

**حدّ صادق:** الإلغاء **تعاوني**. نرفع `signal` ونعلّم الوظيفة ملغاة، لكن لا يمكن
قتل عمل لا يفحص الإشارة؛ المعالجات الطويلة تفحص `ctx.signal.aborted` بين المراحل.

**التحقق:** `check` 72/72 · `lint` نظيف · **429/429 اختبارًا** (كانت 402) ·
مُتحقَّق حيًّا: إحصاءات الطابور، و404 للمجهول، والبثّ بلا ضغط.


---

## 15. ما نُفِّذ — P3: التخزين والمشاريع (2026-09-07)

البنود 29 و30 و40 و59 من خطة المنصة. هذه أول مرة يحتفظ فيها المشروع بحالة تتجاوز
عمر الطلب.

### قراران يستحقان التصريح

**1) SQLite عبر `node:sqlite` المدمج — لا ORM ولا خادم قاعدة.**
SQL حقيقي بمعاملات وفهارس ومفاتيح أجنبية، **بلا أي تبعية وبلا بناء أصلي**.
الأخيرة ليست تفصيلة: الدوكرفايل يوثّق أن اعتمادية أصلية مفقودة (`onnxruntime-node`)
منعت الخادم من الإقلاع أصلًا داخل الحاوية. `better-sqlite3` كان سيعيد ذلك الخطر.
**الحدّ الصادق:** الوحدة **تجريبية** في Node 22 وتُصدر `ExperimentalWarning` عند
الإقلاع، وواجهتها قد تتغيّر. لذلك كل SQL محصور في `server/db/` — الانتقال إلى
`better-sqlite3` أو Postgres يمسّ ملفين لا أكثر.

**2) التخزين بالمفاتيح لا بالمسارات.** كل المستدعين يتعاملون مع مفاتيح
(`projects/<id>/media/<uuid>.mp4`)، والسائق وحده يعرف نظام الملفات. إضافة
S3/R2/Supabase (البند 40) تصبح ملفًا جديدًا وفرعًا في `index.js` بلا لمس مستدعٍ واحد.

### الأمن — نقطة الخطر الحقيقية

مفتاح التخزين **شبيه بمسار**، فتحويله إلى مسار ملف هو بالضبط موضع اجتياز المسار.
دفاعان مستقلان:
1. `normalizeKey` يرفض `..` و`/` البادئة و`C:` والبايت الصفري وفواصل ويندوز
   (`..\` كان سيمرّ من فحص `../` وحده).
2. `resolveInRoot` يتحقّق بعد `path.resolve` أن الناتج ما زال تحت الجذر — يمسك أي
   التفاف عبر روابط رمزية أو تطبيع مختلف من نظام الملفات.
3. **الأهم:** اسم الملف القادم من المستخدم **لا يدخل المفتاح إطلاقًا**. المفتاح
   يُولَّد (`randomUUID`) ويُحفظ الاسم الأصلي في `meta` — فالخطر مُغلق من المصدر لا
   بالتطبيع وحده. اختبار يرفع `../../../etc/passwd` كاسم ويتأكّد أن المفتاح بقي
   داخل مجلد المشروع.

كذلك: قراءة أصل تتحقّق من **انتمائه للمشروع** في المسار؛ بدونها يصير معرّف الأصل
وحده مفتاحًا لأي ملف في النظام (اختبار يحرس ذلك بمشروعين).

### الخصوصية (البند 59) — مُتحقَّق منها حيًّا

حذف المشروع يحذف صفوفه **وملفاته على القرص**. `ON DELETE CASCADE` يتكفّل بالأصول،
لكنه **لا يعمل في SQLite بلا `PRAGMA foreign_keys = ON`** (مُعطّل افتراضيًا) —
مضبوط في `db/index.js` واختبار يؤكّده. الملفات تُحذف **قبل** الصفوف: لو فشل حذف
الملفات نبقي الصفوف، فلا تصير ملفات يتيمة بلا أي مرجع يدلّ عليها.

### ما أُضيف

- `server/providers/storage/` — `keys.js` (التطبيع) + `local.js` (سائق بكتابة
  ذرّية) + `index.js` (الاختيار).
- `server/db/` — `index.js` (اتصال + ترحيلات في معاملات) + `migrations.js`
  + `projects.js` (المستودعات: كل SQL هنا وحده).
- `server/routes-projects.js` — CRUD كامل + رفع الأصول وتنزيلها وحذفها.
- `tests/storage.test.js` (11) + `tests/projects.test.js` (20).

**نطاق مُعلَن:** لا مستخدمين بعد — كل المشاريع مشتركة. إضافة المالك تصبح عمودًا
وفلترًا، ولا تغيّر شكل المسارات.

**التحقق:** `check` 79/79 · `lint` نظيف · **460/460 اختبارًا** (كانت 429) ·
وفحص حيّ كامل: إنشاء مشروع ← رفع أصل ← ظهور الملف على القرص ← حذف المشروع ←
**اختفاء الملف فعليًا**.


---

## 16. الاستوديو — أول مسار مرئي كامل (2026-09-08)

### لماذا هذا العمل تحديدًا

بعد P0.5 وP1 وP2 وP3، كانت **الشاشة التي يراها المستخدم لم تتغيّر إطلاقًا**. كل ما
بُني كان «غرفة محرّكات»: طبقة مزوّدين، وطابور، وقاعدة، وتخزين — أساسات لا يراها أحد.
الخطر كان واضحًا: معمارية ممتازة **بلا منتج**. هذه المرحلة تحوّل كل ما سبق إلى شيء
يُرى ويُستعمل.

### المسار

```
ملف يرفعه المستخدم → مشروع محفوظ → تفريغ صوتي → ترجمة → SRT/VTT للتنزيل
```

كل خطوة تستهلك ما بُني سابقًا: المشاريع والتخزين (P3)، ومحرك الوظائف بسقف
التزامن والتقدّم والإلغاء (P2)، وسلسلة المزوّدين (P1).

- `server/projectPipeline.js` — الخط كوظيفة مسجَّلة في المحرك، بمراحل تُبَثّ.
- `POST /api/projects/:id/process` — يعيد 202 ومعرّف وظيفة (العمل أطول من عمر الطلب).
- `public/studio.html` + `public/js/studio.js` — أربع خطوات مرقّمة، بثّ تقدّم حيّ
  عبر `EventSource` مع **سقوط إلى الاستطلاع** عند انقطاع الشبكة (بدل شاشة معلّقة
  بلا تفسير)، وجدول نتائج، وزرّا تنزيل.

**المصدر ملف يرفعه المستخدم لا تنزيل من يوتيوب** — وهذا هو المسار المتوافق قانونيًا
الذي اختير في §9.1.

### التحقق — في متصفح حقيقي لا باختبارات فقط

قُدت الواجهة فعليًا في Chromium: تحميل الصفحة ← اختيار ملف ← بدء ← **التقاط شريط
التقدّم أثناء عمله** (تفريغ الصوت… 25%) ← ظهور 4 مقاطع مترجمة ← تنزيل SRT
والتحقق من ترقيمه وتوقيته. الخطأ الوحيد في الطرفية كان حجب البروكسي لخطوط Google
في هذه البيئة — لا علاقة له بالتطبيق.

### npm run demo:studio

التفريغ الحقيقي يحتاج ffmpeg ونموذجًا، والترجمة تحتاج شبكة. على جهاز بلا أحدهما
تبدو الواجهة معطّلة وهي سليمة. هذا السكربت يزيّف **خطوتين فقط** (التفريغ والترجمة)
ويبقي كل ما عداهما حقيقيًا: الرفع والتخزين والقاعدة والطابور والتقدّم وبناء
SRT/VTT والتنزيل وحذف المشروع.

**التحقق:** check 81/81 · lint نظيف · **469/469 اختبارًا** (كانت 460) ·
tests/projectPipeline.test.js (9) يغطي الخط كاملًا ومحتوى SRT/VTT وفشل
audio-empty وعزل المشاريع وحذف الملفات.


---

## 17. إعداد النشر — Render Starter + قرص دائم (2026-09-08)

قرار مُتَّخذ: **Render**، لا Vercel. تحقّقتُ من توثيق Vercel الرسمي قبل الحسم: نموذجه
لا يدعم عمليات خادم دائمة ولا عمّالًا خلفيين ولا طوابير — يكسر مباشرة طابور
الوظائف (P2) وبثّ SSE والاتصال الدائم بـSQLite (P3). Render يشغّل حاوية Docker
دائمة، وهو ما بُني عليه المشروع كله من البداية.

### القرار الفعلي: Starter لا Free

**حقيقة مُتحقَّق منها (لا افتراض):** الخطة المجانية في Render بلا قرص دائم إطلاقًا
ونظام ملفاتها **مؤقت** — كل ما بُني في P3 (قاعدة البيانات، ملفات المشاريع) يُمحى
عند كل نوم للخدمة (15 دقيقة خمول) أو إعادة نشر. هذا لا يتوافق مع «مشروع حقيقي
بمشاريع محفوظة». مصدر: [Render Docs — Persistent Disks](https://render.com/docs/disks) ·
[Render Docs — Free](https://render.com/docs/free).

**التكلفة:** $7/شهر (Starter: 512MB رام، 0.5 vCPU) + $0.25/غيغابايت للقرص
(5GB ≈ $1.25/شهر) — **~$8.25/شهر إجمالًا**.

### ما تغيّر في `render.yaml`

- `plan: starter` (كان `free`) + قسم `disk` يُركَّب على `/app/cache` — يطابق
  `VOLUME /app/cache` في `Dockerfile` تمامًا، فلا حاجة لتعديل أي كود: `DB_FILE`
  و`STORAGE_DIR` يشيران إلى `cache/` أصلًا.
- `JOB_CONCURRENCY=1` بدل الافتراضي (2) عمدًا: 512MB رام ضيقة لتشغيل ffmpeg +
  Whisper + Tesseract متزامنين تحت استخدام حقيقي — رفعه يحتاج ترقية الخطة أولًا،
  وموثَّق كتنبيه صريح في الملف نفسه لا في تعليق مدفون.
- أُضيف `YOUTUBE_API_KEY` (عميل يوتيوب الرسمي، P3 §13) و`ADMIN_TOKEN` (لوحة
  التحكم) كمتغيّرين اختياريين — كانا غائبين عن القائمة رغم أن الكود يدعمهما.

**لم أُنشئ خدمة Render فعليًا ولم أرفع أي مفتاح** — هذا يتطلب حساب المستخدم على
render.com وربط المستودع يدويًا؛ الملف جاهز الآن لخطوة «New +» → «Blueprint».

## 18. دمج الاستوديو داخل تبويب «ترجمة ملف» (2026-09-08)

**سبب القرار:** المستخدم رأى الرابط المنفصل «الاستوديو» في الترويسة على النسخة
المنشورة وانتقد القرار مباشرة: صفحة/نظام منفصل لرفع الفيديو/الصوت أسوأ تجربة
مستخدم من دمجه في تدفّق الترجمة الموجود أصلًا. الحجة تقنية لا ذوقية فقط: §16
بنى جدول نتائج مخصّصًا وشريط تقدّم مخصّصًا يكرّران عمليًا كل ما في
`public/js/result.js`/`ui.js` — عمل مضاعف لصيانة واجهتين لمفهوم واحد (نتيجة
ترجمة مع ترجمات مسطورة). القرار: إزالة `studio.html`/`studio.js` كليًا وتوجيه
رفع الملفات عبر خط أنابيب المشاريع (P3) من داخل تبويب «ترجمة ملف» نفسه.

### ما تغيّر

- **`public/js/features.js`**: `handleFile()` يفرّق الآن بين مستند (`kind:
  'document'`، المسار القديم `/api/translate-file`) ووسائط (`kind: 'media'`،
  حدّ 30 ميغابايت بدل 10) عبر قائمتي امتدادات منفصلتين.
- **`public/js/projectUpload.js`** (جديد): يغلّف تدفّق المشاريع الكامل — إنشاء
  مشروع ← رفع أصل ← بدء معالجة (202 + jobId) ← متابعة عبر `EventSource` مع
  سقوط إلى استطلاع (backoff حتى 5 ثوانٍ) عند فشل البثّ — ويصدّر تسميات المراحل
  بالعربية لعرضها في شريط التقدّم **المشترك** (`#progress-label`)، لا شريط
  مخصّص.
- **`public/js/translate.js`**: `runTranslate()` يتفرّع إلى المسار الجديد إن
  كان `state.file.kind === 'media'`، وإلا يسلك مسار المستندات كما كان.
- **`server/projectPipeline.js`**: شكل النتيجة صار `{type: 'local-video',
  videoId: null, projectId, sourceLang, targetLang, captions, subtitles:
  {srt, vtt}, meta: {title, source: 'upload', cached}}` — يطابق ما يتوقّعه
  `renderYouTubeResult()` تمامًا فيُعاد استخدامه حرفيًا، لا نسخة مصغّرة منه.
- **`public/js/result.js`**: أُصلحت علّة كانت كامنة في الكود القديم — النوع
  المُرسَل من الخادم `'local-video'` بينما كان الفرع المطابق يبحث عن
  `'localvideo'` (بلا شرطة)، فكانت `renderLocalVideo` تُستدعى ولا تُعرَّف
  إطلاقًا (خطأ عند التشغيل لم يظهر لأن مسار الاستوديو القديم لم يكن يمرّ من
  هنا). الآن: `renderLocalVideo(data)` يستدعي `renderYouTubeResult(data)` ثم
  يخفي `#result-embed` (لا فيديو يوتيوب لتضمينه).
- حُذف: `public/studio.html`، `public/js/studio.js`. أُعيدت تسمية
  `scripts/demo-studio.js` → `scripts/demo-media.js` (`npm run demo:media`)
  ليشير إلى تبويب «ترجمة ملف» بدل الصفحة المحذوفة.

### علّة CSS حقيقية وُجدت أثناء التحقق (سابقة على هذا الدمج، غير ناتجة عنه)

أثناء قيادة التدفّق فعليًا في Chromium ظهر مربّع خطأ أحمر فارغ وشريط تقدّم بنص
«جاري جلب المحتوى…» على **كل** تحميل صفحة جديد، قبل أي تفاعل. السبب: قواعد مثل
`.progress { display: flex; … }` و`.error-card { display: flex; … }`
و`.field { display: flex; … }` و`.btn-action/.btn-listen/.btn-srt { display:
inline-flex; … }` في `public/style.css` لم تكن تملك مقابل `[hidden] {
display: none; }` — فتتغلّب على سمة `hidden` الافتراضية التي يبدّلها الكود في
`ui.js` (`el.hidden = true/false`، لا `el.style.display`). النمط نفسه كان قد
أُصلح سابقًا لعنصر واحد فقط (`.dub-status[hidden]`) مع تعليق يشرح المشكلة
بالضبط، لكن لم يُطبَّق على البقية. الأثر الفعلي المرصود بأدوات فحص الحوسبة
(computed `display`, لا فقط سمة HTML): `#text-mode` و`#file-mode` يظهران
متراكبين معًا فوق وضع الرابط الافتراضي، و`#smart-btn` يظهر في وضع الرابط رغم
أنه خاص بوضع النص. **لا علاقة لهذا بدمج الاستوديو** — الكود المُدمَج اليوم لا
يلمس `style.css` إطلاقًا، والعلّة موجودة في كل تحميل صفحة بصرف النظر عن أي
تعديل هنا. أُصلحت بإضافة قواعد `[hidden] { display: none; }` المقابلة
لكل واحدة من الأربع، وتحقّقتُ بفحص `computed display` (لا السمة فقط) عبر
Playwright على 31 عنصرًا يحمل `hidden` في الصفحة قبل الإصلاح وبعده: 6 كانت
تُعرَض رغم `hidden` قبل الإصلاح، 0 بعده.

### التحقق — في متصفح حقيقي

قدت التدفّق كاملًا في Chromium: تبويب «ترجمة ملف» ← رفع فيديو ← نفس زر «ترجم
الآن» المستخدم لكل الأوضاع ← شريط التقدّم **المشترك** يعرض «تفريغ الصوت
(الأطول)… 25%» حيًّا ← النتيجة تظهر في لوحة `#result` نفسها بكامل شريط
الأدوات (SRT، VTT، TXT، دبلجة، استماع، مشاركة، نسخ، تشكيل، مقارنة) — أغنى مما
كان جدول الاستوديو المخصّص يعرضه أصلًا ← تنزيل SRT فعليًا عبر `#srt-btn` نفسه
والتحقّق من ترقيمه وتوقيته ومحتواه المترجَم. لا عنصر واجهة مخصّص للاستوديو
تبقّى.

**التحقق:** check 82/82 · lint نظيف · 470/470 اختبارًا. `tests/
projectPipeline.test.js` حُدِّث: تأكيدات على شكل النتيجة الجديد، واختبار
`GET /studio.html → 404` بدل الاختبار القديم لوجود الصفحة.

## 19. الملكية والمصادقة وتصفية الديون (2026-09-08)

بعد الدمج (§18) صار رفع الملفات المسار **الرئيسي** في الصفحة الأولى بدل صفحة
جانبية، فارتفع أثر ثغرة كانت قائمة أصلًا: `/api/projects/*` بلا أي مصادقة.

### ما كان يحدث فعليًا (لا افتراض)

`GET /api/projects` كان مفتوحًا للجميع ويعيد **كل** المشاريع، ومنه معرّفات كل
الأصول. أي زائر لـ`arklink.onrender.com` كان يستطيع: سرد مشاريع الآخرين،
تنزيل ملفاتهم المرفوعة وترجماتها، وحذف مشاريعهم. الكود كان يوثّق هذا كقرار
نطاق («لا مستخدمين بعد — كل المشاريع مشتركة»)، لكن غياب المستخدمين لا يبرّر
انكشاف بيانات من يستخدم الأداة اليوم.

### الحل: توكن مالك (capability) لا حسابات

- ترحيل قاعدة v2: عمود `owner_hash` على `projects`. نخزّن **sha256 للتوكن** لا
  التوكن — تسريب القاعدة لا يمنح وصولًا.
- `POST /api/projects` يولّد 32 بايت عشوائية ويعيدها **مرة واحدة** في
  `ownerToken`؛ لا تظهر في أي قراءة لاحقة (`rowToProject` لا تعرفها).
- كل عملية على مشروع تتطلّب رأس `X-Project-Token` (مقارنة ثابتة الزمن).
- **الرفض 404 لا 403** عمدًا: 403 يؤكّد وجود المعرّف لمن لا يملكه.
- صفّ قديم بلا `owner_hash` ⇒ يُرفض دائمًا (fail-closed).
- `GET /api/projects` (سرد الكل) صار **للأدمن وحده**؛ المستخدم العادي لا يحتاج
  «قائمة» أصلًا — متصفحه يحفظ توكناته.
- الواجهة: `projectUpload.js` يحفظ التوكن في `localStorage` (بسقف 50) ويرسله.

**حدّ صادق:** هذه ليست مصادقة مستخدمين. من يمسح تخزين متصفحه يفقد الوصول إلى
مشاريعه بلا استرجاع، ومن يسرق التوكن يملك المشروع. مقبول لمنتج بلا حسابات،
ويُستبدل بحسابات حقيقية دون تغيير شكل المسارات (دين §8.9).

**ملاحظة مقصودة:** `/api/jobs/:id` لم يُقيَّد بتوكن لأن `EventSource` لا يرسل
رؤوسًا مخصّصة أصلًا؛ معرّف الوظيفة UUID عشوائي (رابط قدرة) ولا يكشف إلا تقدّم
وظيفة واحدة يعرف صاحبُها معرّفَها.

### ديون §8 المصفّاة في الجولة نفسها

- **§8.6 توكن الأدمن في `localStorage`** → `POST /api/admin/login` يبادله بكوكي
  `HttpOnly; SameSite=Strict` (و`Secure` في الإنتاج). جافاسكربت لم يعد يقرأ
  السرّ، فثغرة XSS لا تسرّبه. الرأس `x-admin-token` يبقى مقبولًا للعملاء الآليين
  والاختبارات. المنطق كله في `server/adminAuth.js` بدل تكراره في موضعين.
- **§8.5 ترتيب الوسائط الهشّ** → `tests/middlewareOrder.test.js`: خمسة تأكيدات
  تحرس ما كان محروسًا بتعليقات (SSE قبل الضغط، الملفات قبل `express.json`،
  الثابت بعد الضغط، الحد قبل الموجّهات). كسر الترتيب صار يفشل الاختبار بدل أن
  يعطّل ميزة بصمت.
- **§8.7 `specs/` بلا فهرس** → `specs/README.md` يوسم كل موجة (منفَّذة/جزئية/
  مهجورة) ويحيل إلى `CURRENT_STATE.md` كمرجع حيّ.
- **§8.8 لا تتبّع تراخيص** → `THIRD_PARTY.md` مولَّد من رخص `node_modules`
  الفعلية لا من الذاكرة.

**التحقق:** check 84/84 · lint نظيف · 486/486 اختبارًا (كانت 470) ·
`tests/projectOwnership.test.js` (10) يغطي العزل والكوكي، و
`tests/middlewareOrder.test.js` (5) يحرس الترتيب.

## 20. تحصين مسار الدبلجة + إعادة CI إلى الأخضر (2026-09-08)

بالتوازي مع §19 استقبلت `main` نظام دبلجة يوتيوب v2 من مسار عمل آخر. فحصُه كشف
ثلاث مشاكل حقيقية — كلٌّ منها مُتحقَّق منه في الكود ثم في خادم يعمل، لا استنتاجًا.

### 20.1 CI كان أحمر ولم يكن أحد يعلم

`Actions run 37` (دمجهم `3cb0499`) = فشل، و`run 36` (`554eff6`) = نجاح — أي أن
`main` صارت حمراء عند ذلك الدمج. السبب اختبار واحد: `E2E: mix + mux` يستدعي
`ffmpeg` وCI لا يثبّته (`Dockerfile` يثبّته، فكان CI يخالف الإنتاج).

- `.github/workflows/ci.yml`: خطوة `apt-get install ffmpeg` قبل الاختبارات — الآن
  يعمل الاختبار **فعلًا** في CI بدل أن يفشل.
- `tests/youtube-dub.test.js`: يتخطّى نفسه **معلنًا السبب** على جهاز بلا ffmpeg،
  فلا يبدو المشروع مكسورًا وهو سليم. التخطّي لا يخفي شيئًا لأن CI يثبّت الأداة.

### 20.2 الثغرة نفسها التي أُغلقت في §19، مفتوحة من جديد

ستة مسارات في `routes-youtube-dub.js` بلا أي مصادقة: قراءة حالة مهام الآخرين،
سرد مشاريعهم، **تنزيل الفيديوهات المدبلجة التي أنتجوها**، و**حذف مشاريعهم**
(`DELETE /dub/projects/:pid`). الحارس الوحيد كان معرّفًا من 8 أحرف hex = 32 بت.

الحل بالنمط نفسه لا باختراع جديد:
- `server/ownerToken.js` (جديد): `newToken`/`hashToken`/`verifyToken` بمقارنة ثابتة
  الزمن. **يستخدمه الطرفان** — `db/projects.js` (SQLite) و`dubbing/dub-owner.js`
  (نظام الملفات) — فالمنطق مصدر واحد لا نسختان.
- `server/dubbing/dub-owner.js` (جديد): التجزئة في `owner.json` **مستقلًّا** لا في
  `meta.json`، لأن الأخير يُعاد كتابته أثناء التشغيل ومن عدّة لغات فيُمحى المالك.
- كل مسار مشروع/مهمة خلف حارس ملكية، **والرفض 404 لا 403** (لا نؤكّد وجود معرّف
  لمن لا يملكه)، وتجاوز للأدمن عبر `isAdmin`. مشروع بلا `owner.json` يُرفض دائمًا.
- المعرّفات صارت `randomUUID()` كاملًا بدل 8 أحرف.
- التوكن يُقبل في الرأس، أو في `?token=` للحالات الثلاث التي **لا تستطيع** إرسال
  رؤوس: `EventSource` و`<video src>` و`<a download>`.

### 20.3 حدود المعدّل لم تكن تُنفَّذ إطلاقًا

`app.use('/api/dub', …)` و`app.use('/api/youtube', heavyLimiter)` كانا مركَّبَين
**بعد** `app.use('/api', require('./routes-youtube-dub'))`، فالموجّه ينهي الطلب
قبل بلوغهما: أثقل عملية في المشروع كانت تحت الحد العام وحده. نُقلا قبله (وحُذف
تكرارهما لاحقًا حتى لا يُحتسب الطلب مرتين)، و`tests/middlewareOrder.test.js`
يحرس ذلك الآن — العطل كان غير مرئي لأي اختبار.

### 20.4 سقف الموارد (قرص Render خمسة غيغابايت)

- **تزامن:** كان كل طلب يطلق `setImmediate` لكل لغة (حتى 5) بلا سقف. الآن لغات
  الطلب الواحد **بالتتابع** — وهذا يزيل أيضًا سباقًا حقيقيًا: كلها تتشارك
  `projectId` وتكتب على `source.mp4` و`meta.json` نفسها بلا قفل — وطابور بسقف
  `JOB_CONCURRENCY` للمشاريع المتوازية.
- **مدة الفيديو:** `MAX_VIDEO_MINUTES` كانت تُقرأ ولا تُقارَن بشيء. الآن تُفرَض
  برمز `video-too-long`.
- **التنظيف:** كان يحمي «كل ما عمره أقل من ساعة» — معيار خاطئ في الاتجاهين. صار
  المعيار **وجود مهمة جارية فعلًا** (تُقرأ من `job-*.json`)، والدورية كل ساعة بدل
  6 ساعات، و`PROJECTS_MAX_BYTES` هبط إلى 3GB لأن القرص يتقاسمه `projects/` مع
  `cache/` — سقفٌ مساوٍ لحجم القرص كله يعني أن التنظيف يبدأ بعد فوات الأوان.

### 20.5 لم يُعالَج (خارج النطاق المتّفق عليه)

- **محرّكا وظائف متوازيان:** `jobs/queue.js` (سقف تزامن، إلغاء، TTL) و
  `jobs/job-manager.js` (استمرارية على القرص، بلا إلغاء، مفردات حالة مختلفة) لا
  يعرف أحدهما الآخر. توحيدهما قرار منفصل.
- **`msedge-tts` مطلوبة وغير مثبَّتة:** `edge-tts.js:7` يستوردها، وهي غير معلنة في
  `package.json` وغير موجودة في `node_modules`، بينما `TTS_ENGINE` افتراضه
  `'edge'` — فكل نداء أصوات عصبية يفشل صامتًا ويسقط إلى gTTS. مفارقة: §8.1 يوثّق
  إزالتها في P0.5 كتبعية ميتة. الإصلاح قرار سطر واحد (قلب الافتراضي إلى `gtts`
  أو إعلانها تبعية اختيارية).

**التحقق:** check 97/97 · lint نظيف · **524/525** (والمتبقّي تخطٍّ معلن لـffmpeg) ·
`tests/dubOwnership.test.js` (9) · فحص حيّ على خادم يعمل: تنزيل بلا توكن 404،
وبتوكن خاطئ 404، وبالتوكن 200 بمحتوى صحيح، و`DELETE` بلا توكن 404 **والمشروع باقٍ**،
و`owner.json` غير قابل للتنزيل، وحدّ المعدّل يردّ 429 فعلًا (كان لا يُنفَّذ).

## 21. إصلاحات صحّة في مسار الدبلجة (2026-09-08)

بعد §20 لم يبقَ شيء حرِج. هذه الجولة عن **الصدق لا الأمان**: أشياء تفشل بصمت أو
تُبلغ حالة خاطئة، فتُضلّل من يقرأ السجل أو الواجهة.

### 21.1 مهمة ناجحة كانت تُعرض «منقطعة»

`job-manager.js` عند تجاوز `MAX_JOBS=200` كان يُخرج **الأقدم بصرف النظر عن حالتها**.
ومهمة الدبلجة طويلة بطبعها فهي أقدم ما في الخريطة وأول المرشّحين — وهي تعمل. وبعد
إخراجها يعيد `updateJob` القيمة `null` بصمت: تتوقّف تحديثات التقدّم عن الحفظ، ولا
تُكتب الحالة النهائية، ثم يقرأ `reloadFromDisk` آخر لقطة (`processing`) ويعلّمها
`interrupted`.

**أثبتُّ ذلك تجريبيًا** على نسخة تحمل المنطق القديم: المهمة الجارية أُخرجت فعلًا،
و`updateJob` عليها أعاد `null`. الآن لا يُخرَج إلا ما حالته نهائية؛ وإن لم يوجد
منتهٍ يرتفع السقف مؤقتًا — أهون من إتلاف عمل جارٍ.

### 21.2 أرقام خروج ffmpeg كانت تتسرّب كرموز خطأ

النداءات كانت `execFileAsync('ffmpeg', …)` عارية في ثلاثة ملفات، وNode يضع رقم
الخروج في `err.code`، والمُمسِك الوحيد يفعل `(e && e.code) || 'server-error'` —
فيصير الرمز المعروض `1`. وهي العلّة نفسها التي يوثّق `downloader.js:53-54`
إصلاحها سابقًا. أُضيفت `server/dubbing/ffmpeg.js` تلفّ النداءات وتحوّل الفشل إلى
`ffmpeg-missing` / `ffmpeg-timeout` / `ffmpeg-failed` (مع مقتطف stderr)، ولها
رسائل عربية في `errAr`. المهلة صارت مميَّزة بعد أن كانت تسقط إلى `server-error`.

### 21.3 الأصوات العصبية كانت ميتة بصمت

`edge-tts.js` يستورد `msedge-tts` وهي **لم تكن معلنة في `package.json` ولا مثبَّتة**،
بينما `TTS_ENGINE` افتراضه `edge`. الأثر: `voice-manager` يوزّع صوتًا مختلفًا لكل
متحدث، لكن الجميع يخرج بصوت gTTS واحد — «تعدّد المتحدثين» معطّل عمليًا بلا أثر في
السجل.

- أُعلنت في `optionalDependencies` (لا `dependencies`: فشل جلبها يجب ألّا يُفشل
  التثبيت). **تحقّقت من النسخة من السجل الفعلي** (`2.0.7`) لا تخمينًا، وتحقّقت أن
  واجهة v2 تطابق ما يستعمله الكود (`MsEdgeTTS`/`OUTPUT_FORMAT`/`setMetadata`/`toStream`).
- `package-lock.json` حُدِّث و**تحقّقت أن `npm ci` ينجح** — بدونه كان CI ينكسر لأن
  القفل خارج المزامنة.
- في الكود: `MODULE_NOT_FOUND` صار يعني تعطيلًا **دائمًا** بتحذير مرة واحدة، بدل
  قاطع عشر دقائق يعيد محاولة `require` لن تنجح أبدًا.

**حدّ صادق:** تحقّقت أن الوحدة تُحمَّل وأن الواجهة تُربَط، **ولم** أتحقّق من
توليد صوت فعلي: هذه البيئة تحجب نقطة Microsoft (403 من البروكسي). على Render
بإنترنت كامل يُفترض أن تعمل — وهذا ما يحتاج تأكيدك.

### 21.4 بثّ SSE كان يبقى معلّقًا — واختبار الوحدة لم يكشفه

الخادم لم يكن يستدعي `res.end()` إطلاقًا. أُصلح البثّ ليُغلق عند الحالة النهائية،
وتُحذف مجموعة المشتركين حين تفرغ (كان يبقى مدخل فارغ لكل مهمة بُثَّت، إلى الأبد).

**والأهم منهجيًا:** اختبار الوحدة نجح بينما الفحص الحيّ فشل (`curl -N` انتهى
بمهلة 124). السبب: `broadcast` يُنادى من `updateJob` وحده، فمهمة انتهت **قبل**
الاشتراك لا يقع بعدها أي حدث — وهي الحالة الشائعة (فشل سريع كـ`ytdlp-missing` يقع
قبل أن يفتح المتصفح البثّ). أُضيف إغلاق فوري لهذه الحالة، وأُضيف اختبار HTTP
حقيقي لها لأن الردّ المزيّف لم يكشفها. بعد الإصلاح: رمز الخروج 0.

### 21.5 الاستعادة من القرص كانت تحجب الإقلاع

`reloadFromDisk()` (متزامنة: `readdirSync` + `readFileSync` لكل `job-*.json`)
كانت تُنادى وقت تحميل الوحدة. صارت `setImmediate(reloadFromDisk)` — تفرغ قبل أول
طلب HTTP، وتبقى الدالة متزامنة لأن الاختبارات تناديها مباشرة.

**التحقق:** check 98/98 · lint نظيف · **533/534** (المتبقّي تخطّي ffmpeg المعلن) ·
`tests/dubRobustness.test.js` (9) · `npm ci` ينجح · فحص حيّ على خادم يعمل يؤكّد
إغلاق البثّ ورمز `ytdlp-missing` النصّي.

## 22. إصلاح نتائج المراجعة الشاملة (2026-09-11)

تقرير `تقرير-المراجعة-الشاملة.html` (655 اختبارًا ناجحًا، 0 lint) وجد 6 مشاكل
(3 مكسورة + 3 معرّضة). أُصلحت الخمس القابلة للفعل، مع تصحيحين لادعاءين في
التقرير نفسه بعد التحقق من الكود:

### 22.1 التبعيات: من 11 ثغرة إلى 2 (متوسطتين مقبولتين)

- `npm audit fix` (غير الكاسر) أصلح `@xmldom/xmldom` (عبر mammoth).
- `overrides` في `package.json`: `protobufjs → ^7.6.6` (يغلق الحرجة +
  3 high الأبوية في نفس السلسلة)، `sharp → ^0.35.4`، `qs → ^6.16.0`.
- **تصحيح نسبة:** السلسلة الحرجة تأتي عبر `@xenova/transformers →
  onnxruntime-web → onnx-proto → protobufjs` (مثبت 6.11.6)، **لا** عبر
  `sherpa-onnx` كما قال التقرير — وهي محمّلة كسولًا خلف محرك STT الاحتياطي
  (الافتراضي sherpa). تحقّقت تحميل `onnx-proto` و`@xenova/transformers`
  و`sharp` و`mammoth` و`exceljs` بعد التجاوز — كلها تعمل.
- `uuid → ^11.1.1` أيضًا عبر overrides: تبيّن بالفحص أنّ v11 ثنائي ESM/CJS
  (الـ require يعمل)، فتحقّقت من تحميل exceljs وإنشاء Workbook واختبارات
  الملفات (15/15) — والتدقيق الكامل الآن **0 ثغرات**. `package.json` بلا تعليقات (JSON لا يقبلها) —
  التوثيق هنا بدلها.

### 22.2 الوظائف: قائمة الأدمن + حذف بملكية (مع تصحيح شدة)

- **تصحيح شدة:** `GET /api/jobs` لم يكن «يسرد كل الوظائف» — يعيد عدّادات
  إجمالية فقط (`stats()`: بلا معرّفات ولا أسماء ملفات). صار للأدمن وحده
  (`requireAdmin`) كبيانات تشغيلية، بنفس قاعدة `GET /api/projects`.
- `DELETE /api/jobs/:id` على وظيفة مشروع (`project-video`) يتطلّب توكن
  المالك أو الأدمن (404 لغير المالك — لا تأكيد وجود). الوظائف المجهولة
  المالك (`video-local` العام) تبقى روابط قدرة UUID (منشئها لا يملك توكنًا).
- `GET /:id` والبث يبقيان روابط قدرة **عمدًا وموثّقًا** (CURRENT_STATE §19):
  `EventSource` لا يرسل ترويسات مخصّصة، والمعرّف 128-بت غير قابل للتخمين.
  routes-youtube-dub كان يفرض الملكية أصلًا على مساراته.
- `docs/openapi.json` صُحّح معها (كان يصف `/api/jobs` كقائمة وظائف وهو
  عدّادات) + معامل `ProjectHeader` جديد. `queue.js` كشف `ownerProject(id)`
  (معرّف المشروع من الحمولة دون تسريب base64).

### 22.3 التوثيق: أرقام حقيقية

- README: شارة وجدول `624` → `661`، فحص الصياغة `105` → `106`، ملفات
  الاختبار `60` → `62`، الوكلاء `12×2` → `100×2 = 300 (20 فرقة)`،
  openapi `~42` → `42` (مقاسة فعليًا).
- `SPEC-multi-agent-architecture.md` (36 وكيلًا) وُسم **تاريخي (superseded)**
  مع إحالة إلى `agents/index.js stats` — لم يُعَد كتابته (744 سطرًا تصميم أولي).

### 22.4 الاكتشاف: robots + sitemap + llms.txt

- `public/robots.txt` (يمنع `/admin.html` و`/api/`) + `public/sitemap.xml` +
  `public/llms.txt` (وصف الأداة ونقاطها للوكلاء الذكية). تُخدَم تلقائيًا عبر
  `express.static`، ومغطاة باختبار في `smoke.test.js`.
- **حدّ صادق:** النطاق في sitemap/robots عنصر نائب
  (`aralink.onrender.com` من اسم خدمة `render.yaml`) — بدّله بالنطاق الفعلي
  عند النشر.

### 22.5 قفل الأدمن: fail-closed

- `checkLoginLock` كانت تُعيد `null` (سماح) عند خطأ المخزن. الآن تُعيد
  `{ storeError: true }` فيُجيب `/api/admin/login` بـ **503** — انقطاع Redis
  لم يعد يفتح نافذة تخمين. (حد الطلبات العام يبقى fail-open عمدًا: التوافر
  أولًا للمسارات العامة.)
- الدالة مقحمة المخزن (`store = loginFailStore`) ومُصدَّرة للاختبار —
  `tests/adminLockout.test.js` (3): خطأ المخزن، عدّ 4-ثم-قفل، عزل IP.

### 22.6 إصلاح عرضي: `search_code` كان مكسورًا على Windows

المجموعة الكاملة كشفت فشلًا واحدًا خارج التقرير: `search_code يجد نمطًا`
(agents.test.js) — الأداة تستدعي ثنائية `grep` الغائبة على Windows فتُعيد
0 دائمًا (فشل بيئي سابق للتغييرات — تحقّقت عبر `git stash`). أُضيف سقوط
لمسح Node خالص بنفس الشكل عند غياب الثنائية فقط. **المنهجية:** لا تُخفِ
الفشل — `matches >= 8` بقيت تتحقق من العدد الحقيقي (8 فعلًا).

**التحقق:** check 106/106 · lint نظيف · **661/661** · إقلاع حي + فحص
`/robots.txt` و`/sitemap.xml` و`/llms.txt` (200) و`/api/jobs` بلا توكن (401).
