# CURRENT_STATE.md — تقرير الحالة الراهنة (Phase 0: Repository Audit)

> **الغرض:** الفحص الكامل للمستودع قبل كتابة أي كود، تنفيذًا للقسم 2 والقسم 69 من
> Master Development Prompt. هذا التقرير هو **الأساس الواقعي** لأي قرار معماري لاحق.
>
> **التاريخ:** 2026-09-07 · **الالتزام المفحوص:** `8c3054a` · **الفرع:** `claude/work-review-aefh9k` (مطابق لـ `main`)
>
> **حالة التنفيذ:** أُنجزت **P0.5** و**P1** بعد هذا الفحص (انظر §11 و§12). الأرقام أدناه محدَّثة لتعكسها.
>
> **منهجية الفحص:** قراءة الملفات فعليًا + تشغيل `npm run check` و`npm run lint` و`npm test` و`npm audit`.
> كل رقم أو ادّعاء هنا مُتحقَّق منه بتنفيذ فعلي، لا استنتاج. ما لم يُتحقَّق منه مُعلَّم صراحةً بـ **[افتراض]**.

---

## 1. ما هو موجود (Inventory)

### 1.1 الخادم — `server/` (36 وحدة، 6342 سطرًا)

| الوحدة | الأسطر | الدور |
|---|---:|---|
| `files.js` | 608 | استيراد/تصدير الملفات (11 استيراد → 8 تصدير) |
| `translate.js` | 551 | محرك الترجمة: 6 مزوّدين + كاش + تقسيم + cooldown |
| `routes-sse.js` | 409 | بثّ الترجمة عبر SSE |
| `routes-translate.js` | 380 | مسارات الترجمة (رابط/نص/ذكية/SRT) |
| `audio.js` | 354 | تفريغ صوتي (yt-dlp + ffmpeg + STT) |
| `fetchContent.js` | 304 | جلب المقالات/المواقع/PDF + حماية SSRF |
| `geminiVideo.js` | 301 | ترجمة فيديو يوتيوب عبر Gemini متعدد الوسائط |
| `cache.js` | 252 | كاش ترجمة على ملف JSON بكتابة ذرّية |
| `server.js` | 218 | تركيب Express: CORS، helmet، حد الطلبات، المسارات |
| `ssrf.js` | 211 | حماية SSRF + تثبيت الاتصال على العنوان المُتحقَّق |
| `routes-video.js` | 211 | بثّ الفيديو مع Range + عدّ مراجع |
| `store.js` | 182 | متجر KV قابل للتبديل (ذاكرة/Redis) لعدّادات الحد |
| `context-detect.js` | 178 | كشف نوع المحتوى (تقني/كود/طبي/قانوني…) |
| `languages.js` | 176 | ~130 لغة بأسماء عربية |
| `quality.js` / `wer.js` | 159 / 97 | قياس جودة الترجمة بـ WER مع تطبيع عربي/تركي |
| `envSettings.js` | 159 | قراءة/كتابة `.env` بأمان (allowlist + منع حقن الأسطر) |
| `pdf.js` | 149 | استخراج نص PDF بلا مكتبات (zlib فقط) |
| `stats.js` / `usage.js` / `logger.js` | 139 / 55 / 45 | إحصاءات + عدّاد استخدام + سجل أخطاء بتدوير |
| `tashkeel.js` | 130 | تشكيل عربي (Gemini + احتياطي قواعدي) |
| `routes-local-video.js` | 129 | فيديو محلي base64 → تفريغ → ترجمة |
| `tts.js` | 126 | نطق عبر gTTS + دمج ffmpeg |
| `extractionRules.js` | 98 | قواعد استخراج CSS مخصصة للمواقع الصعبة |
| `routes-*.js` (بقية) | ~430 | file / dub / tts / ocr / tashkeel / stats / settings |
| `downloader.js` | 79 | غلاف yt-dlp عبر execFile |
| `ocr.js` | 76 | OCR عبر tesseract.js (ara+eng) بمهلة 60 ثانية |
| `youtube.js` | 67 | استخراج معرف الفيديو + جلب الترجمات النصية |

### 1.2 الواجهة — `public/` (2651 سطرًا)
- `index.html` (312) + `admin.html` (83) + `style.css`
- وحدات ES: `app.js` (نقطة الدخول), `translate.js`, `result.js`, `media.js`, `dub.js`, `features.js`, `ui.js`, `stream.js`, `dashboard.js`, `utils.js`, `constants.js`
- PWA: `sw.js` + `manifest.webmanifest` + `theme-init.js`

### 1.3 الإضافة — `extension/` (Chrome MV3)
- صلاحية `activeTab` فقط (لا `<all_urls>`) — منضبطة أمنيًا.
- `background.js`: قائمة سياقية تفتح AraLink بـ `?url=`. `popup.js`: زر «ترجم الصفحة الحالية».
- **لا تنفّذ أي معالجة** — تحوّل فقط إلى الويب. (مطابق لمتطلب القسم 35 مسبقًا.)

### 1.4 البنية التحتية والتوثيق
- `Dockerfile` (node:22-slim + ffmpeg + yt-dlp عبر pip3، فحص وقت البناء، healthcheck)
- `docker-compose.yml` (منفذ 3999، volume للكاش) · `render.yaml` (خطة free، healthCheckPath)
- `.github/workflows/ci.yml`: check → lint → test → docker build
- `AGENTS.md`, `CLAUDE.md`, `DESIGN.md`, `README.md`, `CHANGELOG.md`, `plan.md`
- `specs/`: 7 مجلدات موجات (wave0…wave3، audio-pipeline، speed-video-langs، translation-tool)
- `.agents/skills/` + `.claude/skills/`: 27 مهارة

---

## 2. ما يعمل (مُتحقَّق منه بالتشغيل)

| الفحص | النتيجة |
|---|---|
| `npm run check` (فحص صياغة) | **53/53 ملفًا سليمًا** |
| `npm run lint` (eslint `--max-warnings=0`) | **PASS — صفر تحذير** |
| `npm test` | **359/359 اختبارًا ناجحًا، صفر فشل** (39 ملف اختبار) |

**وظائف تعمل فعليًا:**
- **الترجمة:** 6 مزوّدين مسجَّلين (`google`, `mymemory`, `libre`, `gemini`, `deepl`, `zen`) بسلسلة احتياطية + تجميد 60 ثانية بعد 3 إخفاقات + كاش على القرص.
- **البثّ SSE:** `/api/translate-stream` مركَّب **قبل** `compression()` (اختبار يحرس ذلك)، ومحاذاة 1:1 صارمة في كل المسارات.
- **يوتيوب:** ثلاثة مسارات متتالية — Gemini video → ترجمات يوتيوب النصية → تفريغ صوتي (Whisper/sherpa).
- **الملفات:** استيراد 11 صيغة (txt, md, docx, xlsx, csv, srt, vtt, json, xml, epub, pptx) وتصدير 8.
- **الدبلجة:** `/api/dub` بدفعات ≤40 مقطعًا، توازٍ 4، بوابة لغة TTS.
- **OCR:** tesseract.js (ara+eng) بمهلة 60 ثانية عبر `Promise.race` (`ocr.js:35-46`).
- **الأمن:** helmet + CSP، CORS allowlist، حد طلبات على `/api` كامل، SSRF مع تثبيت الاتصال، مقارنة توكن الأدمن ثابتة الزمن، حجب المفاتيح في رسائل الخطأ.
- **خريطة المسارات:** كل نقطة تستدعيها الواجهة لها مسار مسجَّل (`tests/smoke.test.js` يفشل إن انكسر ذلك).

---

## 3. ما لا يعمل / معطَّل

| البند | الحالة | الدليل |
|---|---|---|
| `msedge-tts` | ~~تبعية ميتة~~ — **أُزيلت في P0.5** | كانت مُعلنة وغير مستوردة؛ `tts.js` يستخدم gTTS |
| تنزيل يوتيوب المباشر على السحابة | **محجوب فعليًا** على Render | `downloader.js:57` يكتشف الحجب ويرفع `youtube-blocked` |
| تصدير PPTX / EPUB | **غير مدعوم عمدًا** (يُخرَجان txt/md/docx) | `files.js:15-16` |
| `bench:translate` | لا يعمل في CI عمدًا (يستهلك حصص مجانية) | `AGENTS.md` |
| توثيق TTS | ~~انحراف توثيقي~~ — **صُحِّح في P0.5** | `AGENTS.md:43` صار يذكر gTTS |

---

## 4. ما يحتاج refactor

1. **`server.js` صار موزّع المسؤوليات** (218 سطرًا): تركيب المسارات + حد الطلبات + المصادقة + CORS + معالجة الأخطاء في ملف واحد. ترتيب الوسائط حسّاس جدًا (SSE قبل الضغط، حدود الأجسام قبل `express.json` العام) وموثَّق بتعليقات طويلة — هشّ أمام أي إضافة.
2. **`files.js` (608 سطرًا)** يجمع 11 مستوردًا + 8 مُصدِّرًا في وحدة واحدة بـ `switch` كبير. يجب فصله إلى `importers/` و`exporters/` بواجهة موحّدة.
3. ~~**`translate.js` (551 سطرًا)** يخلط سجل المزوّدين ومنطق كل مزوّد~~ — **عولج في P1**: صار 302 سطرًا، والمزوّدون في `providers/translation/*.js`.
4. **ازدواج منطق البثّ**: `routes-translate.js` و`routes-sse.js` ينفّذان الأنبوب نفسه مرتين (يوتيوب/مقال/نص). أُصلحت المحاذاة بإعادة استخدام `translateBatch`، لكن بقية التدفق ما زال مكرَّرًا.
5. **الوصول وقت التنفيذ كنمط اختبار** (`const f = (...a) => mod.f(...a)`) منتشر لأن التفكيك يجمّد المراجع — حلٌّ صحيح لكنه عرض لغياب حقن التبعيات (DI).

---

## 5. ما ينقص (الفجوة بين الحالي والهدف)

مقارنة بالمنصة المطلوبة في الـMaster Prompt:

| المطلوب | الحالة الآن |
|---|---|
| Auth / حسابات مستخدمين | **غير موجود** — لا مستخدمين، لا جلسات، لا صلاحيات |
| قاعدة بيانات + Models (30 نموذجًا) | **غير موجودة** (قرار مقصود في `CLAUDE.md`: لا DB للـMVP) |
| Job Queue + Workers + استئناف | **غير موجود** — كل شيء داخل طلب HTTP |
| نظام Projects | **غير موجود** — لا حالة تُحفظ بين الجلسات |
| Speaker Diarization | **غير موجود** |
| WhisperX / word-level timestamps | **غير موجود** (الحالي: sherpa-onnx / Xenova whisper-tiny) |
| Translation Memory | **جزئي** — الكاش يخزّن قطعًا مترجمة لكن بلا لغة/مشروع/ثقة |
| Glossary | **موجود جزئيًا** — `applyGlossary` بعد الترجمة فقط، لا يُحترم أثناءها |
| Subtitle: ASS، تصحيح توقيت، سرعة قراءة | **غير موجود** (SRT/VTT فقط) |
| Subtitle Editor (تحرير يدوي) | **غير موجود** |
| Voice Assignment / Cloning | **غير موجود** — صوت واحد لكل لغة |
| Audio Mixing (Replace/Mix/Voice-over) | **غير موجود** — الواجهة تشغّل mp3 بالتتابع |
| مطّ الصوت (`atempo`) لمزامنة التوقيت | **غير موجود** — تحقّقت: لا ذكر لـ`atempo` في المستودع |
| Video rendering / حرق الترجمات | **غير موجود** — يوجد بثّ فقط |
| Storage abstraction (S3/R2) | **غير موجود** — نظام ملفات محلي فقط |
| Batch processing | **غير موجود** |
| AI Quality Review (بعد الترجمة) | **غير موجود** — `quality.js` قياس offline لا مراجعة لكل ترجمة |
| Observability / structured logging | **بدائي** — `console.error` + ملف أخطاء نصي |
| Lip Sync | **غير موجود** (وهو Advanced حسب القسم 24) |

---

## 6. Dependencies الحالية

**إنتاج (18):** `@xenova/transformers`, `cheerio`, `compression`, `cors`, `docx`, `dotenv`, `epub2`, `exceljs`, `express`, `fast-xml-parser`, `helmet`, `jszip`, `mammoth`, `msedge-tts`(ميتة), `sherpa-onnx`, `tesseract.js`, `undici`, `youtube-transcript`
**اختيارية (2):** `redis`, `youtube-dl-exec` · **تطوير (1):** `eslint` · **المُثبَّت فعليًا:** 346 حزمة

### ⚠️ التدقيق الأمني — `npm audit --omit=dev`

**قبل P0.5: 13 ثغرة (1 حرجة + 6 عالية + 6 متوسطة) · بعد P0.5: 11 ثغرة (1 حرجة + 4 عالية + 6 متوسطة)**

| الخطورة | الحزمة | المشكلة |
|---|---|---|
| **CRITICAL** | `protobufjs` | تنفيذ كود عشوائي (عبر `@xenova/transformers` → `onnxruntime-web` → `onnx-proto`) |
| ~~HIGH~~ | ~~`adm-zip` (عبر `epub2`)~~ | **أُزيلت في P0.5** — حُذفت `epub2` وحلّ محلها JSZip بميزانية فكّ ضغط |
| HIGH | `sharp` | CVE-2026-33327/33328/35590/35591 في libvips |
| HIGH | `@xenova/transformers`, `onnxruntime-web`, `onnx-proto` | موروثة |

> **الأهم عمليًا (عولج):** كانت ثغرة `adm-zip` **قابلة للوصول مباشرةً** — `/api/translate-file` يقبل EPUB من المستخدم و`epub2` يفكّه عبر `adm-zip`. أُغلقت في P0.5.
>
> **ما يتبقّى:** كل الثغرات المتبقية العالية والحرجة تأتي من سلسلة واحدة: `@xenova/transformers` → `onnxruntime-web` → `onnx-proto` → `protobufjs`. **قابلية الوصول منخفضة** (تتطلب نموذج ONNX خبيثًا، والنماذج تُجلب من Hugging Face بطلب التطبيق نفسه لا من المستخدم)، والحزمة **محرّك STT احتياطي** فقط (`sherpa-onnx` هو الافتراضي). لا إصلاح أعلى المجرى: `2.17.2` هو الأحدث. الخليفة `@huggingface/transformers@4.x` يُسقط `onnx-proto`/`protobufjs` كليًا — انتقال يستحق التنفيذ لكنه يحتاج تحقّقًا فعليًا من التفريغ الصوتي، فلا يصح ضمّه إلى P0.5 بلا اختبار.

---

## 7. المشاكل المعمارية

1. **العمليات الثقيلة داخل طلب HTTP.** التفريغ الصوتي والدبلجة وOCR وتنزيل الفيديو كلها تعمل داخل دورة الطلب. **تصحيح فني مهم:** هي **لا تحجب** حلقة أحداث Node (تستخدم `execFile` = عملية نظام منفصلة، وtesseract.js يعمل في worker خاص). المشكلة الحقيقية ليست حجب الحلقة بل: **لا سقف للتزامن، لا استئناف، لا إلغاء، ومهلة الطلب تقتل العمل الجاري**. فيديو 8 دقائق عبر Gemini يعني طلب HTTP مفتوحًا 8 دقائق.
2. **الحالة على القرص لا في مخزن مشترك.** الكاش والإحصاءات والاستخدام ملفات JSON محلية ⇒ **لا يمكن التوسّع أفقيًا**. `store.js` حلّ ذلك لعدّادات الحد فقط.
3. ~~**لا طبقة providers حقيقية إلا للترجمة.**~~ — **عولج في P1**: الترجمة وSTT وTTS كلها خلف واجهات في `server/providers/`.
4. **الاعتماد على نقاط نهاية غير رسمية كمسار أساسي.** `translate.googleapis.com` (ترجمة+كشف لغة) و`translate.google.com/translate_tts` (نطق) بلا اتفاقية استخدام.
5. **ازدواج الأنبوب** بين المسار المتدفّق وغير المتدفّق (انظر §4.4).
6. **لا حدود موارد إجمالية.** حد الطلبات لكل IP فقط؛ لا سقف لعدد عمليات ffmpeg/yt-dlp المتزامنة ⇒ عشرة طلبات فيديو متزامنة تستهلك المعالج بالكامل.

---

## 8. Technical Debt

1. ~~`msedge-tts` تبعية ميتة~~ — **أُزيلت (P0.5)**.
2. 11 ثغرة في تبعيات الإنتاج (§6) — **القابلة للوصول أُغلقت (P0.5)**؛ الباقي من سلسلة `@xenova/transformers`.
3. ~~انحراف توثيقي في `AGENTS.md` حول TTS~~ — **صُحِّح (P0.5)**.
4. ~~تعليق قديم في `routes-settings.js:2`~~ — **صُحِّح (P0.5)**.
5. ترتيب الوسائط في `server.js` هشّ ومحروس بالتعليقات لا بالبنية.
6. `public/js/dashboard.js` يحفظ توكن الأدمن في `localStorage` (لا كوكي `httpOnly`).
7. `specs/` يحتوي 7 موجات تاريخية بلا فهرس يوضّح المكتمل من المهجور.
8. لا `THIRD_PARTY.md` ولا تتبّع تراخيص (مطلوب في القسم 45).

---

## 9. خطة الانتقال إلى المعمارية الجديدة

### 9.1 قرار محجوب يسبق كل شيء (Blocking Decision)

قبل أي عمل معماري يجب حسم تعارض صريح داخل المتطلبات نفسها:

- **القسم 63** يقول: «لا تتجاوز access controls… ولا تجعل النظام مصممًا لتجاوز حماية المحتوى».
- **الكود الحالي** (`geminiVideo.js:3-6`) يقول صراحةً إن غرضه **تجاوز حجب يوتيوب لعناوين مراكز البيانات**، و`downloader.js` يرصد الحجب ويسمّيه.

هذان لا يجتمعان. الخيارات:
- **(أ) مسار متوافق:** YouTube Data API الرسمي + محتوى يرفعه المستخدم بنفسه. أبطأ وأضيق وأغلى، لكنه قابل للتشغيل تجاريًا.
- **(ب) أداة محلية/شخصية:** إبقاء المسار الحالي وعدم تقديمها كخدمة عامة.

**لا توجد توصية تقنية صحيحة قبل هذا القرار** — لأنه يحدّد إن كان «YouTube Engine» في القسم 6 يُبنى أصلًا وبأي شكل.

> **القرار المتَّخذ (2026-09-07): المسار (أ) — متوافق.** YouTube Data API الرسمي للبيانات الوصفية + محتوى يرفعه المستخدم بنفسه.
> **الأثر المباشر:** `geminiVideo.js` (تمرير رابط يوتيوب إلى Gemini) و`downloader.js` (yt-dlp على روابط يوتيوب) **لا يصلحان** كأساس للمنتج العام ويجب عزلهما خلف علم صريح أو إزالتهما عند بناء المحرك الجديد. الأولوية تنتقل إلى مسار «ارفع ملفك» الذي يعمل أصلًا (`/api/video-local`).

### 9.2 التسلسل المقترح (بعد حسم 9.1)

| المرحلة | العمل | لماذا الآن |
|---|---|---|
| ~~**P0.5 — عاجل**~~ ✅ | إصلاح الثغرة القابلة للوصول، وحذف التبعيات الميتة، وتصحيح التوثيق | **مُنجَز** — انظر §11 |
| **P1 — Provider Layer** | استخراج `providers/{translation,stt,tts}/` بواجهات موحّدة (القسم 43) | يفتح كل ما بعده؛ لا يكسر أي API قائمة |
| **P2 — Job Queue** | Redis + BullMQ، تحويل المسارات الثقيلة إلى وظائف، SSE للتقدّم (الأقسام 5/26/27) | يحلّ §7.1 و§7.6 معًا، وشرط لأي عمل ثقيل حقيقي |
| **P3 — Storage + DB** | `StorageProvider` + قاعدة بيانات للمشاريع/المستخدمين (الأقسام 29/30/40) | شرط Projects وBatch وAuth |
| **P4 — Transcription** | واجهة STT + WhisperX/diarization خلفها (الأقسام 8/9) | يعتمد على P1 وP2 |
| **P5 — Subtitle + Dubbing** | ASS، تصحيح التوقيت، `atempo`، إسناد الأصوات، المزج (الأقسام 14-22) | يعتمد على P4 (المتحدثون) وP2 (ffmpeg الثقيل) |
| **P6 — Studio UI** | واجهات Projects/Editor (الأقسام 15/46-49) | بعد استقرار البيانات والوظائف |

### 9.3 قواعد ملزمة أثناء الانتقال
- **لا إعادة بناء الموجود** (القسم 3): الترجمة والكاش وSSRF والملفات وOCR تُغلَّف وتُطوَّر لا تُستبدل.
- **لا كسر APIs الحالية** بلا migration — 359 اختبارًا تحرسها، وأي انخفاض في العدد يعني انحدارًا.
- **البوابة بين المراحل:** `npm run check && npm run lint && npm test` خضراء + الميزة تعمل يدويًا قبل الانتقال (القسم 72).

---

## 10. الخلاصة بسطر واحد

المشروع **أنضج بكثير مما يبدو من حجمه**: 359 اختبارًا خضراء، أمان مدروس (SSRF، helmet، توكن ثابت الزمن، كتابة `.env` محصّنة)، وستة مزوّدي ترجمة باحتياطي حقيقي. الفجوة إلى «المنصة» ليست في جودة الكود القائم بل في **غياب أربع طبقات**: الوظائف (Queue)، والتخزين (DB/Storage)، والهوية (Auth)، وطبقة المزوّدين لغير الترجمة — إضافةً إلى **قرار قانوني** يسبقها كلها.


---

## 11. ما نُفِّذ بعد هذا الفحص — P0.5 (2026-09-07)

**الهدف:** إغلاق الضرر القائم فقط، بلا لمس المعمارية وبلا انتظار قرارات لاحقة.

1. **إغلاق ثغرة قابلة للاستغلال عبر رفع ملف.** حُذفت `epub2` (ومعها `adm-zip`) وأُعيدت
   قراءة EPUB فوق `JSZip` الموجودة أصلًا: `META-INF/container.xml` → ملف OPF →
   ترتيب `<spine>` → فصول XHTML. أُضيفت **ميزانية فكّ ضغط صريحة** (64MB لكل ملف)
   ترفض بناءً على الحجم المُعلَن في فهرس ZIP قبل تخصيص الذاكرة ثم تحاسب الإجمالي
   فعليًا — وطُبِّقت على قارئ PPTX أيضًا لأنه يتعرّض للفئة نفسها.
2. **تغطية اختبارية من الصفر لمسار EPUB.** لم يكن له اختبار واحد رغم أنه يستقبل
   ملفات المستخدم. أُضيف `tests/epub.test.js` (14 اختبارًا): ترتيب `spine`، وOPF في
   الجذر، وفكّ الكيانات، وإسقاط `script`/`style`، والفشل الصريح على الأرشيف التالف
   وعلى غياب `container.xml` وعلى `spine` الفارغ، وميزانية فكّ الضغط، وتطبيع المسارات.
3. **إصلاح تسريب اكتُشف أثناء الاختبار:** نص `<script>`/`<style>` كان يتسرّب إلى
   النص المُرسَل للترجمة، ومسافة دخيلة كانت تتصدّر كل فقرة.
4. **حذف `msedge-tts`** — تبعية مُعلنة وغير مستوردة إطلاقًا.
5. **تصحيح انحرافين توثيقيين:** `AGENTS.md` (TTS هو gTTS لا MS Edge TTS)،
   وتعليق `routes-settings.js` (المسار محمي بـ `requireAdmin` + `heavyLimiter`).

**التحقق:** `check` 53/53 · `lint` نظيف بصفر تحذير · **373/373 اختبارًا ناجحًا**
(كانت 359) · إقلاع الخادم و`/api/health` و`/api/languages` (132 لغة) مُتحقَّق منها فعليًا.

**النتيجة الأمنية:** 13 ثغرة → **11**، والأهم أن **الوحيدة القابلة للوصول من مدخلات
المستخدم أُغلقت**. المتبقّي محصور في سلسلة `@xenova/transformers` (انظر §6).


---

## 12. ما نُفِّذ — P1: طبقة المزوّدين (2026-09-07)

**الهدف (القسم 43):** كل مزوّد خارجي يُنفِّذ واجهة موحّدة، فإضافة مزوّد جديد =
ملف واحد + سطر في `index.js` — **بلا تعديل في النواة**. بلا كسر أي API قائمة.

### البنية الناتجة

```text
server/providers/
├── README.md              ← عقد الواجهات الثلاث (TranslationProvider/TTSProvider/TranscriptionProvider)
├── translation/           ← google · mymemory · libre · gemini · deepl · zen  (+ endpoints.js مشترك)
├── tts/                   ← gtts
└── stt/                   ← sherpa · transformers  (+ lang.js مشترك)
```

### الأثر على النواة

| الملف | قبل | بعد | ما بقي فيه |
|---|---:|---:|---|
| `translate.js` | 551 | **302** | الكاش، التقسيم، سلسلة الاحتياطي، التجميد، المسرد، كشف اللغة |
| `audio.js` | 354 | **183** | الملفات المؤقتة، ffmpeg→PCM، اختيار السلسلة، توحيد المقاطع |
| `tts.js` | 126 | **154** | حدود المدخلات، التقسيم على حدود الجمل، دمج ffmpeg |

`tts.js` كبر قليلًا لأنه اكتسب سجلّ مزوّدين حقيقيًا (كان مزوّدًا واحدًا مثبّتًا بالكود).

### قرارات تصميم

- **`isAvailable()` يُقرأ وقت الاستدعاء لا وقت الاستيراد.** قراءتها عند الاستيراد
  تُجمّد القيمة، فيصبح حفظ مفتاح جديد من لوحة الإعدادات بلا أثر حتى إعادة التشغيل
  (`envSettings.saveSettings` يعدّل `config` مباشرةً). موثَّق كقاعدة ملزمة في README.
- **الترتيب في `index.js` هو الأولوية الافتراضية** — لا ترتيب مبعثر في النواة.
- **سلسلة STT معمّمة:** المفضّل في `STT_ENGINE` أولًا ثم الباقي احتياطيًا، والمحرك
  غير المثبَّت يسقط تلقائيًا. الخطأ المُصنَّف (`audio-empty`) يتوقف فورًا، وغير
  المُصنَّف (نموذج/تحميل) ينتقل للمحرك التالي — تعميم للسلوك السابق لا تغيير له.
- **كل مزوّد يملك توحيد مخرجاته:** `toSegments` انتقلت إلى كل محرك STT لأن شكل
  المخرَج خاصية المزوّد لا النواة.

### تغطية اختبارية جديدة على منطق لم يكن مُختبَرًا إطلاقًا

محرّكا التفريغ يحتاجان نماذج وحزمًا أصلية فلا يمكن تشغيلهما في الاختبارات، لكن
**منطق الاختيار والتوحيد نقيّ وقابل للاختبار** — وأُضيف له `tests/sttProviders.test.js`
(16 اختبارًا): السجل، وتحقيق الواجهة، وسقوط المحرك غير المتاح من السلسلة، وحدود
المدة (2–10 ثوانٍ)، ودمج المقاطع الصامتة، وتوحيد مخرجات كل محرك، وتطبيع اللغات.

**التحقق:** `check` 67/67 · `lint` نظيف · **389/389 اختبارًا** (كانت 373) ·
الخادم يقلع و`GET /api/providers` يعيد المزوّدات الستة بنفس الشكل السابق.

### ما لم يتغيّر عمدًا

`LLMProvider` و`StorageProvider` (القسم 43) مؤجَّلان إلى P3 لأنهما بلا مستهلك اليوم:
لا تخزين كائنات ولا طبقة LLM مستقلة عن الترجمة. بناؤهما الآن تجريد بلا حاجة.
