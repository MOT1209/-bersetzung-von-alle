# TEST_CASES.md — حالات اختبار يدوية

قائمة تحقق يدوية لميزة القاموس المحلي والاتجاه (RTL/LTR). التشغيل:

```bash
npm run dev        # ثم افتح http://localhost:3000
```

| # | الحالة | الخطوات | المتوقع |
|---|--------|---------|---------|
| 1 | **ترجمة غير حساسة لحالة الأحرف** | ⚙️ الإعدادات → جرّب القاموس النموذجي «English» → أغلق → تبويب «ترجمة نص سريع» → اكتب `HELLO` ثم `Hello` ثم `hello` | الثلاثة تُترجم إلى «مرحبا» (ملاحظة: الترجمة المحلية تعمل على النتيجة المعروضة فور الاستيراد؛ ولطلب جديد يمر القاموس مع الطلب كمسرد) |
| 2 | **علامات الترقيم محفوظة** | بعد استيراد القاموس: اكتب `Hello, world!` | «مرحبا, عالم!» — الفاصلة والعلامة في موضعهما، والمسافة الأصلية كما هي |
| 3 | **الحفاظ على المسافات** | اكتب `  hello   world` (مسافتان بادئتان، ثلاث بين الكلمتين) | نفس المسافات بالضبط في الناتج، مع ترجمة الكلمتين |
| 4 | **كلمات غير موجودة** | اكتب `hello xyzzy` | «مرحبا xyzzy» — الكلمة الغريبة تبقى كما هي بلا تشويه |
| 5 | **RTL للعربية** | ترجم أي نص إلى العربية | فقرات النتيجة `dir="rtl"`, `text-align: right` — تُقرأ من اليمين |
| 6 | **LTR للإنجليزية داخل الصفحة العربية** | ترجم نصًا عربيًا إلى English | فقرات النتيجة `dir="ltr"` — محاذاة يسار سليمة داخل صفحة RTL |
| 7 | **نص مختلط** | ترجم `مرحبا, Hello!` بعد استيراد القاموس | الترقيم في موضعه؛ كل فقرة تأخذ اتجاه أول حرف قوي فيها |
| 8 | **رفع قاموس صالح** | أنشئ `{"cat": "قطة", "dog": "كلب"}` → 📥 استيراد قاموس JSON | رسالة «تم استيراد 2 مدخلة ✓» والقاموس يعمل فورًا دون إعادة تحميل الصفحة |
| 9 | **رفع JSON غير صالح** | ارفع ملفًا نصيًا عاديًا بامتداد `.json` | رسالة خطأ عربية مفهومة + تفاصيل في `console.error` — لا انهيار |
| 10 | **رفع ملف كبير** | ارفع JSON أكبر من 2MB | رسالة «الملف أكبر من الحد المسموح (2 ميغابايت)» — لا يُقرأ الملف |
| 11 | **قاموس بلا مدخلات صالحة** | ارفع `{"a": 123, "b": null}` | رسالة «ملف غير صالح…» — لا يُدمج شيء |
| 12 | **فشل جلب القاموس النموذجي** | أوقف الخادم ثم اضغط رابط قاموس نموذجي | رسالة «تعذر تحميل القاموس النموذجي…» + `console.error` بالتفصيل |
| 13 | **الحركات العربية** | قاموس بمفتاح `محمد` → اكتب `مُحَمَّد` (مشكول) | يطابق ويرجم — إزالة الحركات مفعّلة افتراضيًا |
| 14 | **Capitalization** | قاموس `{"nasa": "ناسا"}` → اكتب `NASA` | يبقى `NASA` (all-caps يُحفظ)؛ `Nasa` يُترجم طبيعيًا |
| 15 | **مفاتيح خطرة** | ارفع `{"__proto__": "x", "ok": "جيد"}` | يُستورد «ok» فقط — لا تلوث نموذج أولي |
| 16 | **حدود الكلمة** | قاموس `{"cloud": "سحابة"}` → اكتب `cloudy sky` | `cloudy` لا تُترجم جزئيًا |

## سريعة عبر الواجهة البرمجية (بدون متصفح)

```bash
node --test tests/localEngine.test.mjs   # 33 وحدة للمحرك
node --test tests/locales.test.js        # 7 تكامل (يشمل Hello, world! → مرحبا, عالم!)
npm test                                  # كل الحزمة (550+)
```
