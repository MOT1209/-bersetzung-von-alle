# Wave-0 — الإصلاحات الحرجة (الموجة ٠)

حالة: ✅ منفذة (26 أغسطس 2026) — 11 إصلاحًا في 7 مسارات متوازية.

## الهدف
إغلاق 9 أعطال حرجة/عالية + إصلاحين صغيرين في manifest الإضافة، بدون تغيير سلوك المنتج. معيار النجاح: `node --check` 35/35 + `npm test` 232/232 + ترجمة مقال 200 بدل 500 + كاش يُحفظ على OneDrive.

## الخطة
- الموجة 1 (متوازية): A المقالات، B مهلات ffmpeg/m4a، C سقف PDF، D كاش Windows، E الواجهة/الإضافة، F render.yaml، G مهلة يوتيوب
- البوابة: check + test كامل (68s)
- الموجة 2: إيداعات لكل مسار (6 commits)

انظر `requirements.md` للتفاصيل و `tasks/` لكل مسار.
